# # ==============================================================================
# # GitHub Actions - 自动部署到腾讯云服务器
# # 当 main 分支有推送时触发
# # ==============================================================================
# # 
# # 需要在 GitHub 仓库的 Settings → Secrets and variables → Actions 中配置:
# #   - SERVER_HOST:     腾讯云服务器 IP
# #   - SERVER_USER:     SSH 登录用户名 (建议使用专门的部署用户)
# #   - SERVER_SSH_KEY:  SSH 私钥 (对应服务器上的公钥)
# #   - DEPLOY_PATH:     项目在服务器上的路径 (如 /opt/ideas-app)

# name: Deploy to Production

# on:
#   push:
#     branches: [main]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Deploy via SSH
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USER }}
#           key: ${{ secrets.SERVER_SSH_KEY }}
#           script: |
#             cd ${{ secrets.DEPLOY_PATH }}
            
#             # 拉取最新代码
#             git pull origin main
            
#             # 重新构建并启动容器
#             docker compose down
#             docker compose build --no-cache
#             docker compose up -d
            
#             # 清理悬空镜像
#             docker image prune -f
            
#             echo "✅ 部署完成: $(date)"
