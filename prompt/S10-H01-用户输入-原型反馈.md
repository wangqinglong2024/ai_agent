# 33 · H01 用户输入：原型迭代反馈

> **阶段**：H HTML 原型
> **谁来写**：你（评审完原型后）
> **写完落盘**：`docs/S10-prototype/_input/feedback-round<N>.md`
> **下一步**：贴给 AI（系统消息塞 `S00-01`、`S00-03`、`S00-04`、`S10-H02`、X、S、N 全部、当前原型 changelog），AI 直接按 `S10-H02` 输出新一版原型。

---

## 何时使用本模板

- 第一次出原型不需要本模板，直接触发 `S10-H02`。
- 收到原型后**每次**反馈用一份本文件，新建 `feedback-round<N>.md`。

---

## 触发提示词

```
基于 docs/S10-prototype/ 现有 HTML 原型，按 /prompt/S10-H02-AI输出-HTML原型规范.md 出新一版。
读取我下面贴的反馈，逐条改。每条改动写到 changelog.md。
不要重写已稳定页面（仅改受影响页 + 公共 CSS/JS）。
<贴下面填好的反馈>
```

---

## 反馈条目（一条一节）

### F-1
- **页面**：<page-id>
- **位置**：<Block-ID / 选择器>
- **现状**：
- **期望**：
- **理由**（来自 X 哪条原则 / S 哪个 token / 用户测试）：
- **优先级**：必改 / 应改 / 可改

### F-2
- ...

---

## 全局诉求（影响多页）

- `[例：所有页面表格行高从 40 调到 36]`
- `[例：主色加深一档：从 brand-500 改 brand-600]`

> 全局诉求会改动 `01-tokens.md` 与 `styles.css`。AI 应回头同步 docs/S07-design-system/01-tokens。

---

## 验收标准

- 在原型中能复现哪些场景才算通过：
  - SC-1, SC-3 重新走通
  - 移动端 375 下不出现横向滚动
  - 无控制台报错
