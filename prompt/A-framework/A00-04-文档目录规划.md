# 04 · G04 · 文档目录规划（AI 输出落盘约定）

> 本框架所有 AI 产出物统一落到项目根目录 `/docs` 下。本文件给出完整目录树与每个文件的命名约定，AI 必须严格遵守。
>
> 命名总则：
> - B 阶段子目录带 `B0[1-4]-` 前缀（项目级，全局唯一）。
> - C 阶段（产品设计）子目录带 `C0[1-5]-` 前缀，下分 `<feature>/`（每个 feature 一份）。
> - D 阶段（开发）子目录带 `D0[1-3]-` 前缀，下分 `<feature>/`（每个 feature 一份）。
> - A00-meta/ 用于 4 个一次性阶段的澄清问题落盘。
> - 与 `/prompt/` 下 4 个文件夹（A-framework / B-foundation / C-product / D-develop）一一对应。

---

## 一、根目录结构

```
docs/
  A00-meta/
    questions/          ← 所有阶段所有轮次的澄清问答记录
                          F: <字母>-questions-round<N>.md
                          C: <字母>-<feature-id>-questions-round<N>.md
                          以及 *-resolved.md
    changelog.md        ← 跨阶段变更与冻结记录（含 F 层变更影响 feature 列表）
    glossary.md         ← 项目术语表（按需）

  ── F · 一次性定调（项目级）──────────────────────────────────

  B01-architecture/     ← B01 A 阶段产物
    00-index.md         ← 索引与原则速记
    01-tech-stack.md
    02-project-structure.md
    03-database.md
    04-api-conventions.md
    05-coding-standards.md
    06-deploy-env.md
    07-i18n-responsive.md
    08-surfaces.md          ← 应用端（surface）清单与跨端隔离策略（单端项目仅 1 行）
    09-auth-infra.md        ← 鉴权基础设施（Token / 密码 / OAuth / 会话）
    99-open-questions.md
    _input/preferences.md

  B02-permissions/      ← B02 P 阶段产物（已抹除登录与注册具体流程）
    00-index.md
    01-roles.md             ← 角色枚举 + 权限矩阵 + 角色×surface 矩阵
    02-authz-mechanism.md   ← 前端守卫 + 后端拦截 + 行级
    03-authz-data-model.md  ← users/roles/user_roles/audit_logs
    04-auth-feature-guideline.md  ← 「每个 surface 怎么造自己的 auth feature」指南
    99-open-questions.md
    _input/roles-input.md

  B03-ux/               ← B03 X 阶段产物
    00-index.md
    01-direction.md
    02-references.md
    03-anti-examples.md
    04-voice-tone.md
    05-moodboard.md
    06-experience-principles.md
    99-open-questions.md
    _input/style-input.md

  B04-design/           ← B04 S 阶段产物（双子目录）
    design-system/      ← markdown 规范：给前端工程实现 React 组件
      00-index.md
      01-tokens.md
      02-layout.md
      03-navigation.md
      04-status-colors.md
      05-components/
        00-index.md
        01-buttons.md
        02-forms.md
        03-tables.md
        04-modals.md
        05-drawers.md
        06-toasts-alerts.md
        07-empty-loading.md
        08-popovers-tooltips.md
        09-avatars-badges-tags.md
        10-tabs-accordion.md
        11-cards-glass.md
        12-decorations.md
      06-interactions.md
      07-responsive-dark.md
    prototype-style/    ← 可运行 CSS / JS 资产：C04 HTML 原型直接 vendor 引用
      app.css           ← 总入口样式
      app.js            ← 总入口脚本，暴露全局 `bootstrap()` 与组件 API
      tokens.css        ← 与 design-system/01-tokens.md 同源的 CSS 变量
      themes.css        ← light / dark / accent 主题变体
      parts/            ← 拆分后的组件层 CSS / JS、可选
    99-open-questions.md
    _input/visual-input.md

  ── C · 按 feature 循环（每个 feature 一份）─────────────────

  C01-requirements/     ← C01 R 阶段产物（per feature）
    <feature-id>/
      baseline.md       ← 本 feature 的需求基线（核心交付物）
      flows/
        main-flow.md
        exception-flow.md
      99-open-questions.md
      _input/draft.md

  C02-ia/               ← C02 I 阶段产物（per feature）
    <feature-id>/
      00-index.md
      01-feature-catalog.md   ← 功能模块清单（M-ID、一句话职责、主要角色、关联 R-ID）
      02-flows.md             ← 业务流程图（主/异常路径，mermaid）；多于 3 个子流程拆到 flows/
      flows/                  ← 子流程拆分（可选）
        <flow-name>.md
      03-state-machines.md    ← 状态机集中（stateDiagram-v2，SM-ID + 转移表）
      04-pages.md             ← 页面清单（page-id, 名称, 类型, 角色, 关联 R-ID, 关联 M-ID）【不含 URL】
      05-navigation.md        ← 导航结构（菜单层级 + 角色可见性，链接以 page-id 表达）
      06-coverage-matrix.md   ← R-ID × （功能 / 流程 / 状态机 / 页面）覆盖矩阵
      07-error-pages.md       ← 系统兼底页清单（如 401/403/404/500/maintenance 页型，不含路径）
      99-open-questions.md
      _input/page-direction.md

  C03-pages/            ← C03 N 阶段产物（per feature → per page）
    <feature-id>/
      <page-id>.md            ← 单页交互规范
      <page-id>.scenarios.md  ← 场景验证脚本
      99-open-questions.md
      _input/<page-id>-desc.md

  C04-prototype/        ← C04 H 阶段产物（per feature）
    <feature-id>/
      index.html              ← 本 feature 评审入口
      changelog.md
      mock-data.js            ← OP-ID 为键的假数据
      feature.css             ← 仅本 feature 页面级样式（贴 vendor 变量，不重定义 token）
      feature.js              ← 仅本 feature 页面级交互（调 vendor 暴露的组件 API）
      vendor/proto-style/     ← 全量拷自 docs/B04-design/prototype-style/，不修改
        app.css
        app.js
        tokens.css
        themes.css
        parts/
      pages/
        <scope>-<page>.html
      states/
        <page>.empty.html
        <page>.error.html
        <page>.loading.html
        <page>.forbidden.html
      assets/
      _input/prototype-direction.md  ← H01 用户输入（一次性，覆盖本 feature 全部 page-id）
      （H02 澄清清单统一落 docs/A00-meta/questions/H-<feature-id>-questions-round<N>.md）

  C05-prd/              ← C05 E 阶段产物（per feature；同时维护全局索引）
    _global-index.md          ← 全局 feature 索引 + 状态总表
    _glossary.md              ← 全局术语表（首 feature 创建，后续 feature 增订）
    <feature-id>/
      00-index.md
      01-overview.md          ← 本 feature 总览（定位 / 现状 / 版本 / 阅读对象）
      02-glossary.md          ← 本 feature 局部术语表（与 _glossary.md 互补）
      03-personas.md          ← 涉及的用户画像与场景
      04-feature-catalog.md   ← 模块清单（产品视角）
      05-user-journeys.md     ← 关键用户旅程（mermaid）
      06-page-specs/
        00-index.md
        <page-id>.md          ← 单页规格（复述 I / N / H + 截图）
      07-business-rules.md
      08-roles-permissions.md
      09-design-summary.md
      10-known-issues.md
      11-roadmap.md
      12-changelog.md
      99-open-questions.md
      _input/prd-context.md

  D01-data/             ← D01 D 阶段产物（per feature）
    <feature-id>/
      00-index.md
      01-er-diagram.md            ← mermaid erDiagram
      02-entities/                ← 每个实体一个文件
        <entity>.md
      03-business-rules.md        ← 数据层约束 / 触发器规则
      04-validations.md
      05-calculations.md
      06-indexes.md
      07-volume-growth.md
      08-seed-data.md
      99-open-questions.md
      _input/data-rules.md

  D02-api/              ← D02 L 阶段产物（per feature；同时维护全局路由表）
    _global-routes.md         ← 全局路由表（所有 feature 累积，每次新 feature 跑完追加；冲突者低优先级）
    <feature-id>/
      00-index.md
      01-routes-delta.md      ← 本 feature 引入的路由（page-id → URL 映射 + 鉴权要求）
      02-overview.md          ← 资源、URL 前缀、共用 query / header
      03-endpoints/
        <endpoint-id>.md      ← 一个接口一文件，便于编码时只取一份喂 AI
      04-error-codes.md
      05-concurrency.md
      06-events.md            ← 本 feature 触发的事件 / Webhook（如有）
      99-open-questions.md
      _input/operations.md

  D03-validation/       ← D03 V 阶段产物（per feature；上游全冻结后才跑）
    <feature-id>/
      01-upstream-chain.md    ← V01 上游链一致性（本 feature R/I/N/H/E + F 引用）
      02-module-closure.md    ← V02 模块内闭环（本 feature D/L/N/H）
      03-prd-traceability.md  ← V03 PRD 回链校验（本 feature PRD 与全部规范）
```

---

## 二、命名约定

| 对象 | 规则 | 示例 |
|------|------|------|
| feature-id | 全小写，短横分隔 | `order-mgmt` |
| endpoint-id | `<method>-<resource>` | `get-orders`、`post-orders` |
| page-id | `P-<feature>-<seq3>` | `P-order-012` |
| scope | `public` / `app` / `admin` | `app-home.html` |
| 文件名小写 + 短横 | 不要空格、不要中文 | `01-tech-stack.md` |
| 索引 `00-index.md` | 每个有多文件的子目录都要有 | — |
| 待确认 `99-open-questions.md` | 每个子目录都要有 | — |
| 用户输入存档 | `_input/` 子目录 | `_input/draft.md` |
| 全局共享文件 | `_global` / `_global-*.md` 前缀 | `_global-routes.md` |

---

## 三、AI 落盘行为约束

每次 AI 输出最终文件时必须：

1. **声明落盘路径**：在文件头第一行写：
   ```
   <!-- TARGET-PATH: docs/D01-data/order-mgmt/01-tables.md -->
   ```
2. **多文件输出**：若一次产出多个文件，逐个用 `<!-- TARGET-PATH: ... -->` 标注；用户按标注切分保存。
3. **拆分原则**：单文件 ≤ 1200 行；超出按本规划的子目录结构拆。
4. **更新索引**：拆分后必须同时更新对应 `00-index.md`，列出所有子文件 + 一句话职责。
5. **更新全局索引**：C 阶段产物必须同步更新对应的全局索引（如 C05 更新 `_global-index.md`）；D02 阶段路由增量必须同步追加至 `docs/D02-api/_global-routes.md`。
6. **移交问题**：未决问题写入对应子目录的 `99-open-questions.md`，不是塞在正文。

---

## 四、变更与冻结

- **单文件冻结**：在文件头 `冻结状态` 改为 `已冻结`。
- **子目录冻结**：在该子目录 `00-index.md` 顶部写 `冻结状态: 已冻结 · YYYY-MM-DD · 签字: <name>`。
- **变更记录**：所有冻结/解冻写入 `docs/A00-meta/changelog.md`。
- **F 层变更广播**：B01~B04 任一文件变更，须在 `changelog.md` 列出受影响的所有已开工 feature，并触发它们的 D03 V 重跑。

---

## 四.5、多端项目目录变体（multi-surface）

> 仅当 B01 `08-surfaces.md` 中 surface ≥ 2 时启用。单端项目跳过本节，按上面"一、根目录结构"原样使用。

### 触发条件
B01 `08-surfaces.md` 中 surface 数量 ≥ 2。

### 受影响的目录与变体形态

| 阶段 | 单端形态 | 多端形态 |
|------|---------|---------|
| C01 R | `C01-requirements/<feature>/baseline.md` | `C01-requirements/<feature>/baseline.md`（**仍单份**，业务意图整体）+ 可选 `<feature>/<surface>/notes.md`（端特定补充） |
| C02 I | `C02-ia/<feature>/0X-*.md` | `C02-ia/<feature>/_shared/{state-machines.md, flows-shared.md}`（共享真相）+ `<feature>/<surface>/{01-feature-catalog.md, 02-flows.md, 04-pages.md, 05-navigation.md, 06-coverage-matrix.md}`（端特定）|
| C03 N | `C03-pages/<feature>/<page-id>.md` | `C03-pages/<feature>/<surface>/<page-id>.md` |
| C04 H | `C04-prototype/<feature>/...` | `C04-prototype/<feature>/<surface>/...`（每个 surface 独立 index.html / vendor 目录） |
| C05 E | `C05-prd/<feature>/...` | `C05-prd/<feature>/_shared/{glossary.md, business-rules.md}` + `<feature>/<surface>/{00-index.md, 01-overview.md, 06-page-specs/, ...}` |
| D01 D | `D01-data/<feature>/...` | `D01-data/<feature>/...`（**仍单份**，数据模型是单一真相）|
| D02 L | `D02-api/<feature>/...` | `D02-api/<feature>/<surface>/{02-overview.md, 03-endpoints/, 01-routes-delta.md}`，路由强制带前缀 `/api/<surface>/*` |
| D03 V | `D03-validation/<feature>/...` | `D03-validation/<feature>/<surface>/...`（每个 surface 独立 V01/V02/V03，但都引用同一 `_shared/` 与单份 R + D） |

### 共享与端特定的判定原则

- **强一致 → 共享层**（`_shared/`）：状态机、跨端业务规则、术语、数据模型、需求基线
- **端特定 → `<surface>/`**：导航、页面清单、单页交互、原型 UI、端 PRD overview、路由
- **跨端联动场景**：admin 改动触发 app 刷新等场景，**真相**位于 `_shared/state-machines.md`；admin 与 app 各自的 D02 路由调用同一份 D01 表

### auth feature 在多端项目里的形态

每个 surface 必须有自己的 `<surface>-auth` feature，按上表走多端形态：

```
docs/C01-requirements/app-auth/baseline.md
docs/C01-requirements/admin-auth/baseline.md
docs/C02-ia/app-auth/...
docs/C04-prototype/admin-auth/admin/...
```

> auth feature 的 `<surface>` 子目录通常等于 feature 名前缀（`app-auth` 只在 `app/` 下出产物），属于退化的"单端形态"。

---

## 五、跨阶段引用速查

| 阶段产物 | 谁会引用它 |
|---------|----------|
| `B01-architecture/03-database.md` | D01（建表）、D03 V |
| `B01-architecture/04-api-conventions.md` | D02 L、D03 V |
| `B01-architecture/07-i18n-responsive.md` | B04 S、C05 E（技术栈摘要） |
| `B02-permissions/01-roles.md` | C01 R、C02 I、C03 N、D02 L、D03 V、C05 E（权限矩阵） |
| `B03-ux/01-direction.md` | B04 S、C04 H、C05 E（体验摘要） |
| `B04-design/design-system/` | C03 N、前端编码、C05 E（设计摘要） |
| `B04-design/prototype-style/` | C04 H（原型 vendor 拷贝） |
| `C01-requirements/<f>/baseline.md` | 同 feature 的 C02~D03 全部、C05 E |
| `C02-ia/<f>/01-feature-catalog.md` | 同 feature 的 C03 N、C05 E（模块清单源头） |
| `C02-ia/<f>/02-flows.md` + `03-state-machines.md` | 同 feature 的 C03 N（页面状态上下文）、D01 D（状态字段依据）、D02 L（状态转移接口覆盖）、D03 V |
| `C02-ia/<f>/04-pages.md` | 同 feature 的 C03 N、C04 H、C05 E、D02 L（路由映射来源） |
| `C03-pages/<f>/<p>.md` | 同 feature 的 C04 H、C05 E、D01 D、D02 L、D03 V、编码 |
| `C04-prototype/<f>/` | 同 feature 的 C05 E（截图引用）、D01 D、D02 L（推导数据 / 接口）、D03 V、编码 |
| `C05-prd/<f>/` | 同 feature 的 D01 D、D02 L、D03 V、接手者、跨团队评审 |
| `C05-prd/_global-index.md` | 所有后续 feature 启动时查阅 |
| `D01-data/<f>/` | 同 feature 的 D02 L、D03 V、编码 |
| `D02-api/<f>/` | 同 feature 的 D03 V、编码 |
| `D02-api/_global-routes.md` | 后续 feature 的 D02（避免冲突）、所有 D03 V |
| `D03-validation/<f>/` | 进开发前最终闸门；所有 V01/V02/V03 全绿才能写代码 |

---

## 六、新项目初始化命令（参考）

```bash
mkdir -p docs/{A00-meta/questions,\
B01-architecture/_input,\
B02-permissions/_input,\
B03-ux/_input,\
B04-design/{design-system/05-components,prototype-style/parts,_input},\
C05-prd,\
D02-api}
touch docs/A00-meta/changelog.md
touch docs/D02-api/_global-routes.md
touch docs/C05-prd/_global-index.md
touch docs/C05-prd/_glossary.md
```

> `C01-requirements/`、`C02-ia/<f>/`、`C03-pages/`、`C04-prototype/<f>/`、`C05-prd/<f>/`、`D01-data/`、`D02-api/<f>/`、`D03-validation/` 下的 `<feature-id>` 子目录在跑到对应 feature 时再建。
