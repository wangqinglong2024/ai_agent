# 08 · P01 用户输入：角色与授权描述

> **阶段**：P 角色与授权
> **谁来写**：你
> **写完落盘**：`docs/B02-permissions/_input/roles-input.md`
> **上游依赖**：B01 架构（重点 `08-surfaces.md` 应用端清单 + `09-auth-infra.md` 鉴权基础）
> **下一步**：贴给 AI（系统消息塞 `A00-01`、`A00-03`、`A00-04`、`B02-P02`、B01 输出），AI 必须按 `B02-P02` 出澄清问题。

---

## 本阶段做什么、不做什么

| ✅ 本阶段产出 | ❌ 本阶段不做 |
|------|------|
| 全局角色枚举（项目只一份）| 登录页 / 注册页 / 忘记密码页 的字段、交互、文案 |
| 角色 × surface 矩阵（哪些角色出现在哪些端）| 登录方式选型（手机号/邮箱/微信）——那是 feature 级 |
| 多角色与角色组策略 | Token 类型/刷新/存储 —— B01 09-auth-infra 已定 |
| 授权机制（前端守卫 / 后端拦截 / 行级）| 账号过期、被锁、推送重设链接 之类交互细节 |
| 数据可见范围（行级/字段级的原则）| |
| 审计日志、合规、用户/角色数据模型骨架 | |

> **「登录」不是全局事**：每个应用端（surface）都有自己的 auth feature（`app-auth`、`admin-auth`、`merchant-auth`…），它们与「商品」「订单」是同类别的 feature，各自走 C01→C05 循环产出本端的登录/注册/忘密体验，共用 B01 09-auth-infra 定下的鉴权技术栈。

---

## 触发提示词

```
请你扮演"安全/权限工程师"，遵循 /prompt/A-framework/A00-01、/prompt/A-framework/A00-03、/prompt/A-framework/A00-04。
上游：B01 架构规范（已冻结，重点 08-surfaces.md、9-auth-infra.md）。
读取我下面贴的 P 用户输入。
本轮回复必须按 /prompt/B-foundation/B02-P02-AI澄清-权限提问.md 输出澄清清单，
落盘 docs/A00-meta/questions/P-questions-round1.md。
不许直接出权限规范。不许出现任何登录页/注册页/忘密页的交互描述——那是 feature 级。
<贴下面填好的内容>
```

---

## 1. 系统有哪些角色

> 用大白话写每种角色是谁、能做什么级别的事。角色是**全局**枚举，跨 surface 共享（同一个 `ROLE-ADMIN` 可能的用户同时在 admin 与 merchant 出现）。

```
[例：]

1. 老板（最高权限）
   - 看所有数据，做所有操作
   - 管理员工账号
   - 看财务报表

2. 运营（日常操作者）
   - 处理业务数据（订单/商品/库存）
   - 不能看财务
   - 不能管理其他账号

3. 客服（受限）
   - 只读，能登记售后

4. 终端用户
   - 使用业务
```

---

## 1.5 角色 × 应用端（surface）矩阵

> 从 B01 08-surfaces.md 抵 surface-id 列表。标识每个角色**在哪些端出现**（不是"能不能登"，而是"这个端为谁设计"）。同一个角色可出现在多个端。

| 角色 ID | `app` | `admin` | `merchant` | `open` |
|---------|-------|---------|-----------|--------|
| ROLE-USER | ✅ 主 | ❌ | ❌ | ❌ |
| ROLE-MERCHANT | ❌ | ❌ | ✅ 主 | ❌ |
| ROLE-OPS | ❌ | ✅ 主 | ❌ | ❌ |
| ROLE-ADMIN | ❌ | ✅ 主 | ✅ 可代登 | ❌ |
| ROLE-API-CLIENT | ❌ | ❌ | ❌ | ✅ |

> "代登"场景（admin 代商家登录调查）请在备注明确。

---

## 2. 多角色与角色组合

- 一个用户能不能同时拥有多个角色？`[ ] 能  [ ] 不能`
- 是否需要角色组 / 部门概念？`[ ] 需要  [ ] 不需要`
- 是否需要资源级权限（如"只能看自己创建的"）？`[ ] 需要  [ ] 不需要`
- 同一用户跨 surface 是否共享账号？`[ ] 是（共享 users 表 + 多角色担当隔离）  [ ] 各端独立账号  [ ] 由 AI 推荐]`

---

## 3. 权限粒度

- **需要的粒度**：`[ ] 菜单/页面级  [ ] 功能点/按钮级  [ ] 字段级脱敏  [ ] 行级（只能看自己的）]`
- **越权检查位置**：`[ ] 仅前端  [ ] 仅后端（推荐）  [ ] 前+后双重  [ ] 含 DB 行级安全（RLS）]`

---

## 4. 数据可见范围

> 比"能不能调接口"更细的"能看到什么数据"的原则。具体表与字段留 D 阶段。

- `[例：销售只能看自己客户的订单]`
- `[例：教师只能看自己课程的学生]`
- `[例：商家只看自己店铺的订单；platform admin 看全部]`
- `[例：无此类约束]`

---

## 5. 审计日志

- 是否需要：`[ ] 必须  [ ] 可选  [ ] 不需要`
- 必记的操作：`[例：所有写操作 / 仅敏感操作 / 登录登出]`
- 谁能看审计：`[角色]`
- 保留时长：
- 是否需要拆分 surface：`[ ] 同一表加 surface 字段  [ ] 各端独立表]`

---

## 6. 合规要求

- `[例：GDPR / 越南个人数据法 / 中国个保法 / 不要求]`
- `[例：用户可申请导出/删除自己的数据]`

---

## 7. 其他规则

- `[例：管理员不能删除自己]`
- `[例：被禁用的账号登录时给明确提示（具体提示文案在 auth feature 中定）]`
- `[例：暂时无其他规则]`
