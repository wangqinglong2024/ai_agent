# 10 · P03 AI 输出：权限与认证规范模板

> **阶段**：P 权限与角色
> **谁产出**：AI（安全/权限工程师）
> **落盘**：`docs/B02-permissions/` 下多文件

---

## 触发提示词

```
我已答完 P 澄清。请按 /prompt/B-foundation/B02-P03-AI输出-权限规范.md 多文件结构输出到 docs/B02-permissions/。
鉴权技术参数（Token / 密码 / OAuth / 会话）一律引用 B01 09-auth-infra.md，不重复定义。
角色枚举必须与 B02 用户输入 §1 一致，角色×surface 矩阵必须与 B01 08-surfaces.md 一致。
严禁输出任何登录页/注册页/忘密页的流程、字段、文案——那些是 feature 级 `<surface>-auth` 的责任。
未决项写入 99-open-questions.md。
```

---

## 输出多文件清单

```
docs/B02-permissions/
  00-index.md
  01-roles.md                       ← 角色枚举 + 权限矩阵 + 角色×surface
  02-authz-mechanism.md             ← 前端守卫 + 后端拦截 + 行级
  03-authz-data-model.md            ← users/roles/user_roles/audit_logs
  04-auth-feature-guideline.md      ← 「每个 surface 怎么造自己的 auth feature」指南
  99-open-questions.md
```

> **不再产出**：原 02-auth-flow.md、原 05-registration.md（已下沉到各 surface 的 `<surface>-auth` feature，走常规 C01→C05 循环）。

---

## 文件 1：`00-index.md`

```markdown
<!-- TARGET-PATH: docs/B02-permissions/00-index.md -->

# 权限与认证规范 · 索引

> **阶段**：P · 安全/权限工程师
> **上游依赖**：B01 架构规范, B02 用户输入(roles-input.md), P-questions-resolved
> **冻结状态**：未冻结

## 文件清单

| 序号 | 文件 | 职责 |
|------|------|------|
| 01 | 01-roles.md | 角色定义、权限矩阵、角色×surface 矩阵 |
| 02 | 02-authz-mechanism.md | 前端守卫 + 后端拦截 + 行级 |
| 03 | 03-authz-data-model.md | 账号/角色关联/审计的 DB 骨架 |
| 04 | 04-auth-feature-guideline.md | 各 surface 造自己的 `<surface>-auth` feature 的干路指南 |
| 99 | 99-open-questions.md | 未决问题 |
```

---

## 文件 2：`01-roles.md`

```markdown
<!-- TARGET-PATH: docs/B02-permissions/01-roles.md -->

# 角色定义

## 角色枚举

| 角色 ID | 显示名 | 来源（B02 用户输入条目）| 优先级 |
|--------|-------|---------------|--------|
| ROLE-USER | 普通用户 | ... | 1 |
| ROLE-EDITOR | 内容编辑 | ... | 50 |
| ROLE-ADMIN | 超管 | ... | 99 |

## 多角色策略
- 一个用户可拥有多个角色：是 / 否
- 角色冲突时按"优先级"取最高

## 角色 × surface 矩阵（必须存在；单端项目仅一列）

> 引自 B01 `08-surfaces.md` 的 surface-id 列表。同一角色出现在多个 surface 时，下游 `<surface>-auth` feature 各自实现登录入口，但**共享** users 表与本表角色定义。

| 角色 ID | `app` | `admin` | `merchant` | `open` |
|---------|-------|---------|-----------|--------|
| ROLE-USER | ✅ 主 | ❌ | ❌ | ❌ |
| ROLE-EDITOR | ❌ | ✅ 主 | ❌ | ❌ |
| ROLE-ADMIN | ❌ | ✅ 主 | ✅ 代登 | ❌ |

## 全局权限矩阵（页面/功能点级）

| 资源 / 操作 | ROLE-USER | ROLE-EDITOR | ROLE-ADMIN |
|------------|-----------|-------------|-----------|
| 公开页 | ✅ | ✅ | ✅ |
| 个人中心 | ✅(自己) | ✅(自己) | ✅(任意) |
| 课程管理 | ❌ | ✅ | ✅ |
| 用户管理 | ❌ | ❌ | ✅ |
| 报表 | ❌ | ❌ | ✅ |

> 具体到接口的权限校验，落到 L 阶段每个接口节内引用本表。

## 数据可见范围原则

| 角色 ID | 默认可见范围 | 说明 |
|---------|-----------|------|
| ROLE-USER | owner-only | 只看自己创建的 |
| ROLE-MERCHANT | own-shop | 只看自己 shop_id |
| ROLE-ADMIN | global | 全平台 |

## 系统级硬约束
- ROLE-ADMIN 不能删除自己
- 被禁用账号在 authz 层统一返回 403 + 错误码 `AUTH_DISABLED`（具体提示文案在 `<surface>-auth` feature 中）
```

---

## 文件 3：`02-authz-mechanism.md`

```markdown
<!-- TARGET-PATH: docs/B02-permissions/02-authz-mechanism.md -->

# 授权校验机制

> Token / 会话 / 多设备 / 刷新 一律引用 B01 `09-auth-infra.md`，本文件不重述。

## 前端
- 路由守卫：无权限页面如何拦截（统一组件，不同 surface 复用）
- 菜单过滤：按角色 + surface 双维度
- 按钮级控制：隐藏 vs 禁用 的统一规则
- Token 过期处理：统一拦截器 + 触发 `<surface>-auth` feature 的刷新流程

## 后端
- 中间件 / 拦截器
- 校验顺序：鉴权（Token 解析，B01 09-auth-infra）→ surface 校验（aud claim）→ 角色 → 资源所属 → 业务
- 角色信息来源：Token claim `roles[]` + DB 校对
- 越权统一返回：HTTP 403 + 业务错误码 `AUTHZ_DENIED` + 不暴露原因

## 行级 / 字段级（如需）
- 数据库行级安全策略（PG RLS / 应用层 Service 过滤）
- 字段级脱敏规则（按角色返回不同字段子集）

## 审计日志
- 必记字段：操作者 user_id、surface、时间、IP、UA、动作、目标资源、结果
- 存储位置、保留时长（与 B01 09-auth-infra §4 联动，登录类事件由 09-auth-infra 写入；业务越权事件由本机制写入；同表 `audit_logs` + 类型字段）
- 谁能查询（按 `01-roles.md` 中角色×surface 矩阵决定）
```

---

## 文件 4：`03-authz-data-model.md`

```markdown
<!-- TARGET-PATH: docs/B02-permissions/03-authz-data-model.md -->

# 鉴权与授权数据模型

> 这里**只**定义鉴权/授权相关的表。具体业务表在 D 阶段。
> 字段语义须支持 B01 `08-surfaces.md` 的多端 + B01 `09-auth-infra.md` 的 token 策略。

## 表：users

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | uuid | ✅ | 主键 |
| email | text | | 唯一（如启用）|
| phone | text | | 唯一（如启用）|
| password_hash | text | ✅ | 算法见 B01 09-auth-infra §2 |
| status | enum | ✅ | active/disabled/pending |
| created_at, updated_at | | ✅ | |

> 用户**不**与 surface 绑定，跨 surface 共享。

## 表：roles
| 字段 | 类型 | 说明 |
|------|------|------|
| id | text | ROLE-XXX |
| display_name | text | |
| priority | int | |

## 表：user_roles
| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | uuid | |
| role_id | text | |
| surface | text | 该角色生效的 surface（与 B01 08-surfaces.md 对齐）|
| granted_by | uuid | |
| granted_at | timestamptz | |

> 复合主键 (user_id, role_id, surface)；同一 user 同一 role 在不同 surface 上独立生效。

## 表：audit_logs
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | |
| event_type | enum | login/logout/token_refresh/authz_denied/biz_action |
| user_id | uuid | |
| surface | text | |
| ip, ua | | |
| action | text | |
| target | jsonb | 资源类型 + 资源 id |
| result | enum | success/fail |
| created_at | timestamptz | |
```

---

## 文件 5：`04-auth-feature-guideline.md`

```markdown
<!-- TARGET-PATH: docs/B02-permissions/04-auth-feature-guideline.md -->

# `<surface>-auth` feature 编写指南

> **背景**：本框架不在全局规定登录页/注册页/忘记密码。每个 surface 必须**作为一个独立 feature** 走 C01→C05 循环，feature-id 推荐 `<surface>-auth`，例如 `app-auth`、`admin-auth`、`merchant-auth`。

## 1. 该 feature 必须满足的硬约束

- Token / 密码哈希 / OAuth provider / MFA：**全部**复用 B01 `09-auth-infra.md` 的全局决策，不许自定
- 角色枚举：只能从 B02 `01-roles.md` 已声明的角色中映射
- 后端路由前缀：`/api/<surface>/auth/*`
- token claim：必须包含 `aud=<surface>`、`roles[]`、`user_id`
- 审计：登录/登出/Token 刷新/失败必写 `audit_logs`，与 B02 `02-authz-mechanism.md §审计` 共表

## 2. 该 feature 自由决定的部分

- 登录字段（手机号 / 邮箱 / 用户名 / SSO 入口）
- 是否开放自助注册 / 邀请制 / 仅后台开通
- 忘记密码恢复方式（邮箱链接 / 短信验证码 / 联系管理员）
- 登录页 UI、文案、品牌
- 登录失败提示文案（统一错误码 `AUTH_*`，文案 feature 自定）
- 该端是否启用 MFA / 第三方登录（在 B01 09-auth-infra §3 已声明的 provider 范围内挑选）

## 3. 该 feature 走 C 循环时的输出位置

- 多端项目：`docs/C01-requirements/<surface>-auth/baseline.md` 等（按 A00-04 多端目录变体）
- 单端项目：`docs/C01-requirements/auth/baseline.md`

## 4. 校验项（D03 验证阶段使用）

- [ ] feature 内未私自重定义 token 类型 / 哈希算法
- [ ] feature 路由全部位于 `/api/<surface>/auth/*`
- [ ] 涉及的角色全部存在于 B02 `01-roles.md`
- [ ] 该角色在该 surface 上 ✅ 出现于 `01-roles.md` 角色×surface 矩阵
```

---

## 文件 6：`99-open-questions.md`

```markdown
<!-- TARGET-PATH: docs/B02-permissions/99-open-questions.md -->

# 待确认问题

| 编号 | 问题 | AI 默认方案 | 影响 |
|------|------|-----------|------|
```

---

## 输出质量自检

- [ ] 5 份文件全部产出（不再产出 02-auth-flow.md / 05-registration.md）？
- [ ] 角色枚举与 B02 用户输入 §1 完全一致？
- [ ] 角色×surface 矩阵与 B01 08-surfaces.md 的 surface-id 列对齐？
- [ ] 全局权限矩阵覆盖所有项目级业务意图中的功能/页面类目？
- [ ] **未**重定义 Token / 密码哈希 / OAuth / 会话（违规即重写，全部引用 B01 09-auth-infra）？
- [ ] **未**输出任何登录页 / 注册页 / 忘记密码的字段、流程、文案？
- [ ] 04-auth-feature-guideline.md 已明确告知后续 surface 各自走 C 循环？
- [ ] 未决项不在正文，集中在 99 文件？
