# 26 · H01 用户输入：原型说明 / 反馈

> **阶段**：H HTML 原型
> **谁来写**：你（首版前 / 每轮评审完）
> **写完落盘**：`docs/C04-prototype/<feature-id>/_input/round<N>.md`（首版 N=0，迭代 N=1,2,...）
> **下一步**：贴给 AI（系统消息塞 `A00-01`、`A00-03`、`A00-04`、`C04-H02`、`C04-H03`、B03、B04、本 feature C02 IA、本 feature N 全部 + 当前原型 changelog<迭代轮才需要>）。AI 必先按 `C04-H02` 出澄清，再按 `C04-H03` 出原型。

---

## 0. 本轮类型（必填）

```
[ ] 首版（round 0）：从零基于 N + B03 + B04 出第一版静态原型
[ ] 迭代（round N≥1）：基于已有 round (N-1) 原型 + 反馈条目出新一版
```

---

## 触发提示词

```
请你扮演"HTML 原型工程师"，遵循 /prompt/A-framework/A00-01、A00-03、A00-04，硬性遵守 docs/B03-ux/ 与 docs/B04-design/design-system/。
本轮类型：<首版 / 迭代>，feature-id=<>，round=<N>。
上游：本 feature C01-R 基线、本 feature C02-IA、本 feature N 全部页面规范、B03、B04（已冻结）；迭代轮另读 docs/C04-prototype/<feature-id>/changelog.md。
读取我下面贴的 H 用户输入。
本轮回复必须先按 /prompt/C-product/C04-H02-AI澄清-原型提问.md 输出澄清清单，落盘 docs/C04-prototype/<feature-id>/_input/H-questions-round<N>.md；用户答完后再按 C04-H03 出原型 + changelog。
本阶段是产品设计阶段，禁止涉及任何接口 / 路由 / 数据库 / 字段 / 后端实现。
<贴下面填好的内容>
```

---

## 1. 本轮目标（一句话）

```
[首版例] 把 N 阶段已冻结的 6 个 page 全部出成可点击 HTML，覆盖默认/加载/空/错四态。
[迭代例] 修复 round1 评审里 12 条反馈，重点是表格视觉密度 + 移动端横向滚动。
```

---

## 2. 上游冻结清单（用于 AI 自检）

- 本 feature C02 IA 已冻结：[ ] 是  [ ] 否（路径：docs/C02-ia/<feature-id>/）
- 本 feature N 阶段已冻结的 page-id 列表（**首版必填**，迭代可只列本轮涉及）：
  ```
  P-001, P-002, ...
  ```
- B03、B04 已冻结：[ ] 是
- 迭代轮：当前原型路径与版本：`docs/C04-prototype/<feature-id>/`（最新 changelog round = ）

---

## 3-A. 首版专用：页面优先级与状态覆盖

> 仅"首版"轮填写。

| page-id | 名称 | 本轮要不要做 | 必须覆盖的状态 | 备注（如：移动端优先 / 公开页需 SEO+GEO） |
|---------|------|------------|--------------|------------------------------------|
| P-001 | | 是 / 否 | 默认 + 加载 + 空 + 错 | |
| P-002 | | | | |

- 公共骨架（topbar / sidebar / footer）需要在哪些页出现？
- 是否需要"原型说明 README"给评审看？

---

## 3-B. 迭代专用：反馈条目（一条一节）

> 仅"迭代"轮填写。

### F-1
- **页面**：<page-id>
- **位置**：<Block-ID / 选择器>
- **现状**：
- **期望**：
- **理由**（来自 B03 哪条原则 / B04 哪个 token / 用户测试反馈）：
- **优先级**：必改 / 应改 / 可改

### F-2
- ...

---

## 4. 全局诉求（可选，影响多页）

- `[例：所有页面表格行高从 40 调到 36]`
- `[例：主色加深一档：从 brand-500 改 brand-600]`

> 全局诉求若涉及 token 变更，必须同步回写 `docs/B04-design/design-system/01-tokens.md` 与 `docs/B04-design/prototype-style/tokens.css`，再重新拷贝 vendor/；禁止直接在 feature.css 改。

---

## 5. 验收标准

- 在原型中能复现哪些场景才算本轮通过：
  - SC-1, SC-3 重新走通（来自 N 阶段 `<page-id>.scenarios.md`）
  - 移动端 375 下不出现横向滚动
  - 无控制台报错
  - （首版）4 状态截图齐全
  - （迭代）所有"必改"条目逐条对照 changelog 可见

---

## 6. 想问 AI 的（可选）

```
```
