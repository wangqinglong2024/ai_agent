# 26 · H01 用户输入：原型方向

> **阶段**：H HTML 原型
> **谁来写**：你（产品经理 / 交互设计师）
> **写完落盘**：`docs/C04-prototype/<feature-id>/_input/prototype-direction.md`
> **下一步**：贴给 AI（系统消息塞 `A00-01`、`A00-03`、`A00-04`、`C04-H02`、`C04-H03`、B02、B03、B04、本 feature C01-R 基线、本 feature C02-IA 全部、本 feature C03-N 全部页面规范），AI 必须按 `C04-H02` 出澄清问题。

---

## 本阶段做什么、不做什么

| ✅ 本阶段产出（产品设计阶段）| ❌ 本阶段不做（开发件，留给后续开发阶段） |
|------------------------------|----------------------------------|
| 一整套零依赖、可双击运行的静态 HTML 原型 | 真实接口 / endpoint / SQL / 表名 / 列名 |
| 每个 P0 页面的默认/加载/空/错四态 | 真实路由路径（页面间跳转用 `<page-id>.html` 文件锚） |
| `mock-data.js`（按 OP-ID 组织假数据，字段用业务语言） | 后端字段名 / 数据库 schema |
| `vendor/proto-style/` 全量拷自 B04 设计系统的 `prototype-style/` | 任何对 token / 组件 CSS / 全局交互 JS 的修改（必须回 B04 改源） |
| 移动端 / 桌面端断点表现 | 框架 / 打包器 / npm 依赖 |
| 文案与 B03-ux/04-voice-tone.md 一致 | 真实第三方账号 / 真实支付链路 |

---

## 触发提示词

```
请你扮演"HTML 原型工程师"，遵循 /prompt/A-framework/A00-01、/prompt/A-framework/A00-03、/prompt/A-framework/A00-04，硬性遵守 docs/B02-permissions/、docs/B03-ux/、docs/B04-design/design-system/、docs/B04-design/prototype-style/。
本阶段任务：把本 feature 已冻结的 C02 IA + C03 N 页面规范，落成一整套零依赖、双击 index.html 即可跑的静态 HTML 原型，**不写**任何接口 / 路由 / 字段 / 数据库。
feature-id=<>。
上游：本 feature C01-R 基线、本 feature C02-IA、本 feature C03-N 全部页面规范、B02、B03、B04（已冻结）。
读取我下面贴的 H 用户输入。
本轮回复必须按 /prompt/C-product/C04-H02-AI澄清-原型提问.md 输出澄清清单，
落盘 docs/A00-meta/questions/H-<feature-id>-questions-round1.md。
本阶段是产品设计阶段，禁止涉及任何接口 / 路由 / 数据库 / 字段 / 后端实现。
<贴下面填好的内容>
```

---

## 1. feature 身份

> 本节由你从上游冻结产物里抄过来即可。

- **feature-id**：
- **feature 名称**：
- **所属 surface**（来自 B01 08-surfaces.md；多端 feature 同名复用时，本轮原型只产出一个 surface 的）：
- **关联 R-ID 范围**（来自 C01-R 基线 §4 需求清单）：
- **关联 page-id 列表**（来自 C02 04-pages.md，全量列出；多端项目仅列本 surface 下的 page-id）：
  ```
  P-001, P-002, P-003, ...
  ```
- **关联 SM-ID 列表**（来自 C02 03-state-machines.md / `_shared/state-machines.md`；如无可留空）：
- **主要角色**（来自 B02 01-roles.md，且需在角色×surface 矩阵中此 surface 上 ✅）：
- **B03 / B04 是否已冻结**：`[ ] 是`
- **本原型与现有 feature 的入口关联**（来自 C01-R01 §15.4，复述确认；无写"无"）：

---

## 2. 这个原型要解决什么

> 一句话。比"做出原型给评审看"更具体——"让评审在 5 分钟内点通'课程购买 → 学习 → 评价'主链路，并能切换 4 状态查看异常表达"。

```
```

---

## 3. 必出页面与必盖状态

> 把 §1 列出的 page-id 全部排进来。**不分批不分期**，本表里写"是"的页面就是本轮要全部出齐的范围。

| page-id | 名称 | 优先级 | 必盖状态 | 备注（移动优先 / 公开页需 SEO+GEO / 是否暗黑 / 是否权限页） |
|---------|------|-------|---------|--------------------------------------------|
| P-001 | | P0 | 默认 + 加载 + 空 + 错 | |
| P-002 | | P0 | 默认 + 加载 + 空 + 错 | |
| P-003 | | P1 | 默认（4 态可选） | |

> P0 = 必盖 4 态；P1 = 至少出默认态，其它状态视场景选；权限相关页另加 forbidden 态。
> **没有 P2 / 待定 / 二期 这种说法**——要么进表（必做），要么不进表（不做）。

---

## 4. 公共骨架与导航

- 顶栏 / 侧栏 / 底栏在哪些页面出现：
- 是否需要"原型导航首页 `index.html`"列出所有页面 + 状态切换：`[ ] 是（默认）`
- 主题切换器是否本轮上：`[ ] 上  [ ] 不上`（暗黑模式来自 B04，不在 feature.css 重写）
- 是否需要"原型说明 README.md"给评审/客户看：`[ ] 是  [ ] 否`

---

## 5. 交互真实度

| 维度 | 选项 | 你的选择 |
|------|------|---------|
| 表单校验 | 仅前端校验 / 校验 + 模拟提交 loading + toast | |
| 列表分页 | 静态分页 / 模拟懒加载 | |
| 弹窗 / 抽屉 / 下拉 / 吐司 | 全部走 vendor `proto.*` API（默认） | |
| 跳转 | 用 `<page-id>.html` 相对路径锚（**禁用任何真实 URL 路径**） | |
| 数据来源 | 全部从 `mock-data.js` 取，键 = OP-ID（**禁用真实接口字段名**） | |
| 动效 | 与 B03 一致；`prefers-reduced-motion` 是否本轮纳入：`[ ] 是  [ ] 否` | |

---

## 6. 全局视觉诉求（可选，影响多页）

> 任何 token 调整必须回写 `docs/B04-design/design-system/01-tokens.md` 与 `docs/B04-design/prototype-style/tokens.css`，再重新拷贝 vendor/。**禁止**直接在 feature.css 改 token。

- `[例：表格行高从 40 调到 36]`
- `[例：主色从 brand-500 改 brand-600]`

---

## 7. 响应式与可访问性

- 必须无破的断点：`[ ] 375  [ ] 768  [ ] 1280`（建议三档全选）
- 移动端关键改变（哪些块折叠 / 哪些 CTA 固定底部）：
- 键盘可达 / `:focus-visible` 焦点环 / `<label for>`：默认全部要求满足
- ARIA 角色与标签的特殊要求：
- 是否有需要"无权限态（forbidden）"的页面：列出 page-id：

---

## 8. 文案

- 主标题 / 主 CTA / 错误兜底文案：与 `docs/B03-ux/04-voice-tone.md` 对照表一致（默认）
- 是否有 feature 专属术语需 AI 注意：

---

## 9. SEO / GEO（如适用，仅公开页）

> 公开页才填；非公开页留空。

- 公开页 page-id 列表：
- SEO meta（title / description / og:* / 结构化数据需求）：
- GEO 摘要段（≤ 150 字，便于 AI 抓取作答）：
- 禁止 AI 引用的内容：

---

## 10. 验收标准

> 在原型中能复现哪些场景才算本轮通过。直接抄本 feature 各页 `<page-id>.scenarios.md` 中的 SC-ID。

- 走通的关键场景：SC-1（来自 P-001）、SC-3（来自 P-002）……
- 移动端 375 下不出现横向滚动
- 无控制台报错
- P0 页面 4 状态截图齐全
- `vendor/proto-style/` 与 `docs/B04-design/prototype-style/` **字节级一致**
- 全文未出现任何接口名 / SQL / 表名 / 列名 / 真实路由路径

---

## 11. 不确定的事

```
```
