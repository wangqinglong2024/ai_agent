# 32 · N03 AI 输出：页面交互规范模板

> **阶段**：N 页面交互
> **谁产出**：AI（页面交互设计师）
> **落盘**：`docs/C03-pages/<feature-id>/<page-id>.md` + `docs/C03-pages/<feature-id>/<page-id>.scenarios.md`

---

## 触发提示词

```
我已答完 N 澄清。请按 /prompt/C-product/C03-N03-AI输出-页面交互规范.md 输出本页两份文件，
落盘到 docs/C03-pages/<feature-id>/<page-id>.md 与 .scenarios.md。
所有 token 引用 docs/B04-design/design-system/01-tokens.md 中的变量名（不要直接写 hex）。
所有操作以本页内 OP-ID 命名（API-ID 留待 D02 L 阶段在 OP-ID 上反向映射；本阶段不得直接引用 docs/D02-api/）。
所有角色引用 docs/B02-permissions/01-roles.md 里的 ROLE-ID。
未决问题写到当前文件 §99。
```

---

## 文件 1：`<page-id>.md`

```markdown
<!-- TARGET-PATH: docs/C03-pages/<feature-id>/<page-id>.md -->

# 页面：<名称> (`<page-id>`)

> **page-id**：<>（URL 路径由 D02 L 阶段在 docs/D02-api/<feature>/01-routes-delta.md 映射，本文件不写）
> **类型**：list / detail / form / ...
> **关联 R-ID**：R-XXX, R-YYY
> **关联 M-ID**（来自 C02 01-feature-catalog）：M-XXX
> **涉及状态机 SM-ID**（来自 C02 03-state-machines；可为空）：SM-XX
> **关联 OP-ID**：OP-1, OP-2（API-ID 由 D02 L 阶段反向映射；本文件不得引用 D02 产物）
> **角色可见**：ROLE-USER, ROLE-ADMIN
> **上游**：本 feature C01-R 基线（docs/C01-requirements/<feature-id>/baseline.md）、docs/B02-permissions/、本 feature C02-I（docs/C02-ia/<feature-id>/，重点 04-pages.md、03-state-machines.md、01-feature-catalog.md）、docs/B03-ux/、docs/B04-design/design-system/、N 用户输入
> **严禁引用**：docs/B01-architecture/、docs/D01-data/、docs/D02-api/、docs/D03-validation/（C 与 D 隔离铁律）

---

## 1. 用户目标
- 一句话：

## 2. 进入条件与初始数据
- 进入条件：
- 初始拉取：API-X
- 加载策略：阻塞 / SSR / 懒加载

## 3. 信息架构（区块布局）

```
+------------------------------------------+
| 顶部导航（来自 docs/B04-design/design-system/03-navigation.md） |
+------------------------------------------+
| Hero  [标题][副标题][主CTA]               |  ← Block-1 (首屏权重 1)
+------------------------------------------+
| Tab：概览 / 章节 / 评价                   |  ← Block-2
+------------------------------------------+
| 内容区                                   |  ← Block-3
+------------------------------------------+
| 推荐                                     |  ← Block-4
+------------------------------------------+
| 移动端固定底部 CTA                        |  ← MobileBlock
+------------------------------------------+
```

| Block-ID | 名称 | 职责 | 主要组件（引用 docs/B04-design/design-system/05-components/） | OP-ID | 隐藏条件 |
|---------|------|------|------------------------|-------|---------|

## 4. 操作清单

| OP-ID | 名称 | 主/次 | 触发 | 期望服务端能力（一句话）| 角色 | 反馈方式 | 边界 |
|-------|------|------|------|---------------------|------|---------|------|
| OP-1 | 加入课程 | 主 | Block-1 主按钮 | 校验登录 → 写入「我的课程」 | ROLE-USER | Toast 成功 + 跳学习页 | 未登录→登录 |

> 「期望服务端能力」描述操作期望后端做什么，不出现 endpoint / SQL / 字段名。D02 L 阶段会基于 OP-ID 反向定义 API-ID。

## 5. 状态四件套

### 默认
- 描述、视觉、文案

### 加载中
- 骨架屏 / Spinner / 进度
- 何时显示、何时消失

### 空数据
- 触发：
- 视觉：插画 + 文案
- 主操作：

### 错误
- 触发：HTTP 4xx/5xx / 网络
- 视觉：行内提示 / 全屏错误 / Toast
- 重试策略

## 6. 角色差异

| 角色 | 看到的差异 | 隐藏的元素 |
|------|----------|-----------|

## 7. 表单（如有）
- 字段表（必填/校验/错误位置）
- 提交流程（mermaid 时序）
- 未保存提示

## 8. 键盘 / a11y
- Tab 顺序
- 快捷键
- ARIA 角色与标签
- 焦点管理（弹窗打开/关闭）

## 9. 移动端
- 断点行为
- 主 CTA 形态
- 隐藏元素

## 10. 性能
- 首屏阻塞 ≤ N 个请求
- 懒加载策略
- 图片格式与尺寸

## 11. SEO（公开页）
- title 模板
- description 模板
- 结构化数据（如有）

## 12. 文案
- 主标题：
- 主 CTA：
- 错误兜底：（链 X 04-voice-tone 对照表）

## 99. 待确认
- [ ] ...
```

---

## 文件 2：`<page-id>.scenarios.md`

```markdown
<!-- TARGET-PATH: docs/C03-pages/<feature-id>/<page-id>.scenarios.md -->

# 场景验证 · <page-id>

> 至少 5 条。每条都必须可被人手动复现 / 自动化用例化。

## SC-1：<场景名>
- **GIVEN**：
- **WHEN**：
- **THEN**：
- **涉及 OP-ID / SM-ID 转移**（API-ID 由 D02 L 反向映射）：
- **断言**：
  - 视觉：
  - 数据：
  - 跳转（page-id）：
  - 副作用：

## SC-2 ~ SC-N

## 异常场景必须包含
- [ ] 网络失败
- [ ] 超时
- [ ] 无权限
- [ ] 数据为空
- [ ] 重复操作（防抖/幂等）
- [ ] 弱网慢加载（骨架时间 > 3s）
```

---

## 输出质量自检

- [ ] 两份文件都产出？
- [ ] Block-ID、OP-ID、SC-ID 都有？
- [ ] 4 状态全部描述？
- [ ] OP → 期望能力、角色 → docs/B02-permissions/01-roles.md、token → docs/B04-design/design-system/01-tokens.md 全部可追溯？
- [ ] 没有出现 docs/B01-architecture/、docs/D01-data/、docs/D02-api/ 任何引用？
- [ ] 场景 ≥ 5 且覆盖 6 类异常中至少 4 类？
- [ ] 单文件 ≤ 1200 行？
