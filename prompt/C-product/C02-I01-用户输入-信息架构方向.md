# 20 · I01 用户输入：信息架构方向

> **阶段**：I 信息架构（产品经理视角）
> **谁来写**：你（产品经理 / 业务负责人）
> **写完落盘**：`docs/C02-ia/<feature-id>/_input/page-direction.md`
> **下一步**：贴给 AI（系统消息塞 `A00-01`、`A00-03`、`A00-04`、`C02-I02`、本 feature R 基线、B02、B03、B04 索引），AI 必须按 `C02-I02` 出澄清问题。

---

## 本阶段做什么、不做什么

| ✅ 本阶段产出（产品经理视角）| ❌ 本阶段不做（开发件，留给后续开发阶段） |
|------------------------------|----------------------------------|
| 功能模块清单（M-ID 树）       | URL / 路由路径                    |
| 业务流程图（mermaid）         | API endpoint                      |
| 状态机（订单几个状态、谁触发谁）| 表结构、字段、索引                |
| 页面清单（page-id + 名称 + 类型）| 单页元素布局                     |
| 导航结构（菜单层级 + 角色可见）| HTML / CSS                        |

> **多端项目**：流程图/状态机原则上**跨 surface 共享**（落 `_shared/`），页面清单与导航**按 surface 拆分**。本输入按本 feature 涉及的每个 surface 各填一段 §4/§5；§2/§3 写共享层。

---

## 触发提示词

```
请你扮演"信息架构师"，遵循 /prompt/A-framework/A00-01、/prompt/A-framework/A00-03、/prompt/A-framework/A00-04，硬性遵守 docs/B03-ux/ 与 docs/B04-design/design-system/。
本阶段任务：从产品经理视角输出本 feature 的【功能清单 + 流程图 + 状态机 + 页面清单 + 导航结构】，**不输出 URL / 路由 / 接口 / 表结构**。
读取我下面贴的 I 用户输入。
本轮回复必须按 /prompt/C-product/C02-I02-AI澄清-信息架构提问.md 输出澄清清单，
落盘 docs/A00-meta/questions/I-<feature-id>-questions-round1.md。
<贴下面填好的内容>
```

---

## 1. 功能模块粗描（产品视角）

> 用户能感知到的"功能块"，不是代码模块。一行一条，每条标"主/次"。

```
[例：]
- (主) 课程浏览：用户找到并查看课程
- (主) 订单与支付：把课程"装进我的"
- (主) 学习进度：每个用户每节课的进度
- (次) 收藏与分享
- (次) 评价与评分
```

---

## 2. 关键业务流程（用大白话写完整步骤）

> 至少 1 条主流程 + 1 条异常路径。AI 会画 mermaid，你只需把每一步说清楚：谁、做什么、结果如何分叉。

```
[主流程：用户购买并开始学习]
1. 用户在课程详情点"加入课程"
2. 系统判断是否登录 → 未登录跳登录页，登录后回原页
3. 进入订单确认页，选支付方式
4. 系统调用支付，等待回调
5. 支付成功 → 写入"我的课程" → 跳学习页第一节
6. 支付失败 → 提示原因 + 重试入口
```

```
[异常流程 1：支付超时]
- ...
```

---

## 3. 业务对象的状态流转（状态机）

> 每个有状态的"东西"列一份。比如：订单、课程、学习进度、审核单。

```
[例：订单状态]
- 状态枚举：待支付 / 已支付 / 已完成 / 已退款 / 已取消
- 待支付 →（支付成功）→ 已支付
- 待支付 →（30 分钟超时 或 用户取消）→ 已取消
- 已支付 →（用户开始第一节课）→ 已完成
- 已支付 →（用户在 7 天内申请退款）→ 已退款
- 终态：已完成 / 已退款 / 已取消
- 不可回退：已支付 不可回到 待支付

[例：课程状态]
- 草稿 → 待审核 → 已上架 → 已下架
- ...
```

> **没有状态的对象不必填**。复杂状态机请尽量描述触发条件、谁能触发、是否可回退。

---

## 4. 你已经想好的页面清单（如有）

> 不要写 URL，只写"页面名 + 一句话职责 + 主要服务的角色"。

```
1. 首页：用户进来看到的第一屏，承载推荐与导航
2. 课程列表：按分类/搜索找课程
3. 课程详情：单课程信息 + 加入按钮
4. 我的课程：登录用户看到的学习清单
5. 学习页：单节课的视频/内容播放
6. 订单确认 / 支付结果
...
```

---

## 5. 导航与可见性偏好

- **顶部导航 vs 侧边导航**：`[ ] 顶部  [ ] 侧边  [ ] 双层  [ ] 由 AI 推荐]`
- **后台主结构**：`[ ] 侧边一级菜单  [ ] 顶部一级 + 侧边二级  [ ] 由 AI 推荐]`
- **是否有 dashboard 首页**：`[ ] 是  [ ] 否`
- **哪些页公开可访问**：
- **哪些页登录后才能访问**：
- **哪些页需要特定角色**：

---

## 6. 多语言（仅做产品决策，URL 表达留给开发阶段）

- 是否需要多语言：`[ ] 是  [ ] 否`
- 语言切换的入口位置：

---

## 7. 想问 AI 的

```
```

---

## 8. 元信息（多端项目必填）

- **feature ID**：
- **涉及 surface 列表**：`[与 C01-R01 §0 一致；多端示例：admin, app]`
- **本 feature §2/§3 落点**：`[ ] _shared/（推荐，跨 surface 共享）  [ ] 仅 <surface>/`
- **本 feature §4/§5 落点**：`[ ] <surface>/ 拆分（多端默认）  [ ] 单端 <feature>/ 直接放`

---

## 9. 与现有冻结模块的关联（必填，没有就写"无"）

> 与 C01-R01 §15 一致；本节聚焦"信息架构层"的关联。

### 9.1 复用的现有状态机
| 现有 SM-ID | 来源 feature | 复用方式 |
|---------|-----------|---------|
| `[例：SM-product]` | `[product-mgmt]` | `[直接复用，本 feature 不新增转移；落到本 feature _shared/state-machines.md 的"复用清单"段]` |

### 9.2 复用的现有页面 / 入口
| 现有 page-id | 复用方式 |
|------------|---------|
| `[例：P-admin-product-001]` | `[新增按钮入口跳转到本 feature 的 P-admin-xxx]` |

### 9.3 共享导航变更
- 是否需要在已有菜单/导航插入新入口：`[ ] 是  [ ] 否`
- 插入位置：`[例：admin 端侧边二级菜单 "运营 > 售后管理"]`
