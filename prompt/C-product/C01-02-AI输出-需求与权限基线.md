# C01-02 AI 输出：需求与权限基线

> **阶段**：C01·RP 需求权限（按系统）
> **输入**：`C01-01(同系统)` + `B01` + `B02(同系统)`
> **落盘**：`docs/<system-id>/C01-requirements/<module-id>/<feature-id>/baseline.md`

---

## AI 必须遵守

1. **只读**：C01-01 输入 + 已有冻结基线 + B01 + B02(同系统) + 本模板
2. **不许做**：技术选型、流程图、状态机、UI 草图、字段、接口
3. 存疑写 §99
4. R-ID 格式 `R-<module>-<seq3>`，唯一
5. **增量融合**：有冻结基线时合并，标注 `[本轮新增]`/`[本轮变更]`
6. **权限分离**：角色与权限矩阵作为文件二独立输出
7. 无权限变更时文件二写 `> 本轮无权限变更`
8. 严格遵守 A00-03「输出效率」：表格优先、零废话、描述一句封顶、空则写"无"

---

## 触发提示词

```
按 /prompt/C-product/C01-02-AI输出-需求与权限基线.md 产出两个文件。
系统：<system-id>。落盘 docs/<system-id>/C01-requirements/。
本轮功能「<功能名>」，主动指出融合点和冲突点。
```

---

## 文件一：功能需求基线

> 落盘：`docs/<system-id>/C01-requirements/<module-id>/<feature-id>/baseline.md`

```markdown
# 需求与权限基线

> **阶段**：C01
> **系统**：<system-id>
> **冻结状态**：未冻结
> **本轮聚焦功能**：<功能名>
> **权限文件**：见 docs/<system-id>/C01-requirements/permissions/

## 0. 摘要
- 给谁 / 做什么 / 最大价值 / 最大风险

## 1. 产品定义与目标
### 1.1 一句话定义
### 1.2 业务目标
| ID | 目标 | 度量口径 | 验证时点 |
### 1.3 用户类型
| 类型 ID | 名称 | 画像 | 核心诉求 |

## 2. 需求清单
| R-ID | 标题 | 描述 | 涉及角色 | 依赖 R-ID | 验收标准 | 增量标记 |
|------|------|------|---------|----------|---------|---------|

## 3. 业务场景（叙述，不画流程图）
### 3.1 主要场景
### 3.2 隐含重点能力（供 C02 拆解）

## 4. 业务规则
| 规则 ID | 描述 | 来源 R-ID | 边界值 |

## 5. 角色引用（仅列 ID，不定义）

## 6. 审计与合规
## 7. 第三方集成
| 集成 ID | 名称 | 用途 | mock/真实 | 失败兜底 |

## 8. 非功能性需求
| 类型 | 指标 | 验收口径 |

## 9. 风险登记
| 风险 ID | 描述 | 影响 | 概率 | 缓解 |

## 10. 增量融合报告
### 10.1 本轮新增摘要
### 10.2 融合点
### 10.3 冲突点
### 10.4 已有内容变更

## 11. 暗依赖警示

## 99. 待确认问题
```

---

## 文件二：权限更新

> 落盘：`docs/<system-id>/C01-requirements/permissions/` 下 `roles.md` / `matrix.md`。增量维护。

```markdown
# 权限注册表

> 增量更新，标注 [本轮新增] / [本轮变更]。

## P1. 角色枚举（roles.md）
| 角色 ID | DB/JWT 字面量 | 显示名 | 职责 | 不能做 | 首次引入功能 | 增量标记 |

### P1.1 多角色策略

## P2. 权限矩阵（matrix.md）
| 资源/操作 | ROLE-USER | ROLE-ADMIN | 数据范围 | 所属功能 | 增量标记 |

### P2.2 授权校验机制
- 前端：路由守卫、菜单过滤、按钮控制
- 后端：中间件拦截
- 行级/字段级：RLS、脱敏

## P3. 本轮权限变更摘要
| 变更类型 | 对象 | 说明 | 来源功能 |
```

---

## 质量自检

- [ ] 每条需求有唯一 R-ID？
- [ ] 文件一不含角色枚举/权限矩阵？
- [ ] 无流程图/状态机/URL/路由/页面布局？
- [ ] §10 融合报告完整？
- [ ] 文件二已声明有/无权限变更？
- [ ] 全文<=1200 行？
