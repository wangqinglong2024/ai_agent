# C02-01 用户输入：信息架构与交互

> **阶段**：C02·IN 交互（按系统）
> **系统**：`<system-id>`
> **落盘**：`docs/<system-id>/C02-ia-interaction/<module-id>/<feature-id>/`
> **上游**：C01(同系统已冻结) + B02(同系统)

---

## 做/不做

| ✅ 产出 | ❌ 不做 |
|--------|---------|
| 功能模块清单（M-ID） | URL/路由 |
| 业务流程图（mermaid） | API endpoint |
| 状态机 | 表结构/字段/索引 |
| 页面清单（page-id） | HTML/CSS |
| 导航结构+角色可见 | 数据库 SQL |
| 每页交互规范 | |

---

## 触发提示词

```
扮演"信息架构师+交互设计师"。
上游：C01(同系统已冻结) + B02(同系统)。
读取下面输入，按 /prompt/C-product/C02-02-AI输出-信息架构与交互规范.md 输出。
落盘 docs/<system-id>/C02-ia-interaction/<module-id>/<feature-id>/。
本轮：系统「<system-id>」模块「<module-id>」功能「<feature-id>」。
仅产出该系统，禁止涉及接口/路由/数据库。
<贴下面内容>
```

---

## 0. 元信息

| 字段 | 值 |
|------|----|
| **当前系统** | `[system-id]` |
| 功能 ID | |
| 所属模块 | `[已有 M-ID 或"新模块"]` |
| 需求来源 | `docs/<system-id>/C01-requirements/<module>/<feature>/baseline.md` |

---

## 一、信息架构方向

### 1. 功能模块粗描
> 用户感知的"功能块"，标"主/次"。

### 2. 关键业务流程
> ≥1 主流程 + 1 异常路径。分叉写清判断条件和失败处理。

### 3. 业务对象状态流转
> 每个有状态的对象：状态枚举、转移条件、触发者、终态、不可回退。

### 4. 页面清单（如已想好）
> 不写 URL，只写页面名+职责+角色。

### 5. 导航与可见性偏好
- **导航形态**：`[ ] 顶部  [ ] 侧边  [ ] 双层  [ ] 由 AI 推荐`
- **dashboard**：`[ ] 是  [ ] 否`
- 公开页 / 登录后页 / 特定角色页：

---

## 二、页面交互描述

### 页面 A：`[page-id 或临时名称]`

#### A.1 身份
- page-id / 名称 / 类型(list/detail/form/dashboard/wizard) / 关联 R-ID / 角色

#### A.2 用户目标（一句话）

#### A.3 骨架（5-7 区块名+职责）

#### A.4 所有操作（标主/次）

#### A.5 状态四件套

| 状态 | 触发条件 | 应表达 | 主操作 |
|------|---------|-------|-------|
| 默认 | 正常加载 | | |
| 加载中 | 数据未到 | Skeleton | — |
| 空数据 | 列表为空 | 提示+引导 | |
| 错误 | 加载失败 | 错误+重试 | |

#### A.6 角色差异

#### A.7 场景（GIVEN/WHEN/THEN，≥5，必含异常）

#### A.8 表单（如有）
- 字段名+必填/校验/错误位置/提交方式/未保存提示

#### A.9 移动端

---

## 三、关联与融合

### 1. 复用的现有状态机/页面/入口
### 2. 与已有架构产出的融合说明

---

## 填写自检

- [ ] 流程≥1 主+1 异常？
- [ ] 状态机有终态？
- [ ] 每页有状态四件套和≥5 条场景？
- [ ] 无 URL/接口/字段/表名？
