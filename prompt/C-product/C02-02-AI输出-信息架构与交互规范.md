# C02-02 AI 输出：信息架构与交互规范

> **阶段**：C02·IN 交互（按系统）
> **输入**：`C02-01(同系统)` + `C01(同系统已冻结)` + `B02(同系统)`
> **落盘**：`docs/<system-id>/C02-ia-interaction/<module-id>/<feature-id>/`

---

## AI 必须遵守

1. **只读**：C02-01 输入 + C01(同系统) + B02(同系统) + 同系统已有 C02 + 本模板
2. **不许做**：URL/路由/API/表字段/HTML/视觉设计
3. **不跨系统引用**
4. 存疑写 §99
5. 流程图/状态机一律 mermaid
6. ID：页面 `P-<system>-<module>-<seq3>`、模块 `M-<module>-<seq3>`、状态机 `SM-<module>-<seq3>`
7. 每轮一个系统一个功能，00-index.md 末尾增量融合报告列融合点/冲突点，标注 `[本轮新增]`/`[本轮变更]`
8. 严格遵守 A00-03「输出效率」：表格优先、零废话、场景 GIVEN/WHEN/THEN 各一句、空则写"无"

---

## 触发提示词

```
按 /prompt/C-product/C02-02-AI输出-信息架构与交互规范.md 输出。
落盘 docs/<system-id>/C02-ia-interaction/<module-id>/<feature-id>/。
本轮：系统「<system-id>」模块「<module-id>」功能「<feature-id>」。
需求来源：docs/<system-id>/C01-requirements/<module-id>/<feature-id>/。
R-ID 必须在覆盖矩阵落点。严禁 URL/路由/API/表字段。
```

---

## 输出文件清单

```
docs/<system-id>/C02-ia-interaction/<module-id>/<feature-id>/
  00-index.md
  01-feature-catalog.md
  02-flows.md
  03-state-machines.md
  04-pages.md
  05-navigation.md
  06-coverage-matrix.md
  07-error-pages.md
  99-open-questions.md
```

---

## 01-feature-catalog.md

```markdown
# 功能模块清单

| M-ID | 模块名 | 功能 | 职责 | 主要角色 | 关联 R-ID | 增量标记 |

## 模块×角色矩阵
```

---

## 02-flows.md

```markdown
# 业务流程图

## FL-<module>-001 <功能名>：<主流程名>

涉及模块：M-XXX / 涉及状态机：SM-XXX

```mermaid
flowchart LR
  A1[起点] --> B1{判断}
  B1 -->|是| C1[操作]
  B1 -.->|否| D1[异常处理]
```

## 流程清单
| FL-ID | 名称 | 类型 | 涉及模块 | 涉及 SM | 增量标记 |
```

---

## 03-state-machines.md

```markdown
# 状态机

## SM-<module>-001 <对象>状态

```mermaid
stateDiagram-v2
  [*] --> 初始状态
  初始状态 --> 终态: 触发条件
```

| 转移 ID | 起态 | 终态 | 触发者 | 条件 | 前置校验 | 后置动作 | R-ID | 增量标记 |

终态/不可回退声明

## 覆盖检查
- [ ] 每状态有出/入边或为终态
- [ ] 每转移标了触发者
- [ ] 无不可达/死循环
```

---

## 04-pages.md（核心）

```markdown
# 页面清单与交互规范

> 页面 ID：`P-<system>-<module>-<seq3>`。禁止接口/路由/字段。

## 页面总览
| page-id | 功能 | 名称 | 类型 | 角色 | M-ID | R-ID | SM-ID | 增量标记 |

---

## P-XXX 页面名

### 用户目标
### 进入条件与初始数据形态
### 信息架构（区块布局 ASCII）

| Block-ID | 名称 | 职责 | OP-ID | 隐藏条件 |

### 操作清单
| OP-ID | 名称 | 主/次 | 触发 | 期望反馈 | 角色 | 边界 |

### 状态四件套（默认/加载中/空数据/错误）
### 角色差异
### 场景验证（≥5，必含异常）
| SC-ID | GIVEN | WHEN | THEN | 涉及 OP/SM |

### 表单（如有）
### 键盘/a11y
### 移动端
### 文案
```

---

## 05-navigation.md

```markdown
# 导航结构与角色可见性

> 用 page-id，不出 URL。

## 顶部导航
| 项 | page-id | 可见角色 | 排序 | 增量标记 |

## 侧边导航（如有）
| 一级 | 二级 | page-id | 可见角色 | 增量标记 |

## 用户菜单
## 移动端导航
```

---

## 06-coverage-matrix.md

```markdown
# R-ID 覆盖矩阵

| R-ID | 描述 | M-ID | page-id | FL-ID | SM-ID | 增量标记 |

## 未覆盖检查
- [ ] 所有 R-ID 落点？
- [ ] 所有 M-ID 承接≥1 个 R-ID？
- [ ] 所有 page-id 有≥1 条场景验证？
```

---

## 07-error-pages.md

```markdown
# 系统兜底页

| page-id | 类型 | 触发条件 | 文案 | 主操作 |
|---------|------|---------|------|-------|
| P-<system>-error-001 | 401 | 未登录 | … | 去登录 |
| P-<system>-error-002 | 403 | 无权限 | … | 返回首页 |
| P-<system>-error-003 | 404 | 找不到 | … | 返回首页 |
| P-<system>-error-004 | 500 | 服务器错误 | … | 重试 |
```

---

## 增量融合报告（写在 00-index.md 末尾）

```markdown
## 增量融合 · 第 N 轮 · 功能「<功能名>」

### 本轮新增
| 类型 | ID | 说明 |

### 融合点 / 冲突点 / 已有变更 / 导航更新
```

---

## 质量自检

- [ ] 落盘在 `docs/<system-id>/C02-ia-interaction/`？
- [ ] 06-coverage-matrix 所有 R-ID 落点？
- [ ] 状态机封闭？
- [ ] 每页完整（区块/操作/状态四件套/场景≥5）？
- [ ] 无 URL/路由/API/表字段？不跨系统？
- [ ] 页面 ID 带 system 前缀？单文件<=1200 行？
