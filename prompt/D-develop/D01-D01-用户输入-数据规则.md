# 32 · D01 用户输入：单功能数据规则描述

> **阶段**：D 数据模型（**按 feature 重复**）
> **谁来写**：你（产品 / 业务）
> **写完落盘**：`docs/D01-data/<feature-id>/_input/data-rules.md`
> **下一步**：贴给 AI（系统消息塞 `A00-01`、`A00-03`、`A00-04`、`D01-D02`、本 feature R 基线、B01 输出、B02 输出），AI 必须按 `D01-D02` 出澄清问题。

---

## 触发提示词

```
请你扮演"数据建模师"，遵循 /prompt/A-framework/A00-01、/prompt/A-framework/A00-03、/prompt/A-framework/A00-04。
上游：本 feature C01 R 基线 + 本 feature C02 IA（重点 03-state-machines.md作为状态字段枚举来源） + B01 03-database + B02 03-authz-data-model.md + 本 feature C04 原型 + C05 PRD（已冻结）。
本功能 ID：<feature-id>，对应 R-ID：<R-001, R-002, ...>
读取我下面贴的 D 用户输入。
本轮回复必须按 /prompt/D-develop/D01-D02-AI澄清-数据提问.md 输出澄清清单，
落盘 docs/A00-meta/questions/D-<feature-id>-questions-round1.md。
不许直接出数据规范。
<贴下面填好的内容>
```

---

## 1. 功能信息

- **feature-id**：`[例：course / order / lesson-progress]`
- **功能名称**：
- **关联 R-ID**：`R-001, R-002, ...`
- **业务目标一句话**：

---

## 2. 业务对象与中文术语

> 这个功能涉及哪些"东西"，每个用一句话说清。术语会变成表名或字段。

```
[例：]

- 课程（Course）：……
- 章节（Chapter）：……
- 课时（Lesson）：……
- 学习进度（Progress）：……
```

---

## 3. 每个对象的关键属性（粗）

> 不必给类型，只列名词。

```
[例 课程：]
- 名称、副标题、封面图、简介、难度、语言、定价、是否上架、上架时间、章节列表
```

---

## 4. 对象之间的关系

```
[例：]
- 课程 1对多 章节
- 章节 1对多 课时
- 用户 多对多 课程（通过订单）
- 用户 1对多 学习进度（每个课时一条）
```

---

## 5. 状态字段补充说明

> 业务对象的状态枚举与转移在 C02 IA `03-state-machines.md` 已定义。本阶段只补充「状态字段怎么落到表」的偏好：

- 状态字段与 SM-ID 映射：
  - `course.status` ← SM-02（C02 03-state-machines.md）
  - `order.status` ← SM-01
- 偏好：字段名、是否在 DB 加 CHECK、枚举存为文本还是原生 enum 类型、是否需要状态变更历史表

> 如果发现 C02 SM 不满足需求，不要在本阶段改，必须先回 C02 修订。

---

## 6. 业务规则与边界

> 所有"必须 / 不能 / 唯一 / 至少 / 最多"。

```
[例：]
- 同一用户对同一课时只有一条进度记录
- 课程上架后名称不可改
- 删除课程不允许有未完成订单
```

---

## 7. 计算字段 / 派生字段

```
[例：]
- 课程评分 = 评分总和 / 评分次数（实时算 vs 缓存？）
- 课程总时长 = sum(课时.duration)
- 学习进度 = 已完成课时数 / 总课时数
```

---

## 8. 历史 / 版本 / 软删除

- 是否需要保留历史版本：`[ ] 是  [ ] 否`
- 是否需要软删除：`[ ] 是  [ ] 否`
- 谁能看到已删除：

---

## 9. 量级与增长

| 项目 | 第一年量级 | 三年量级 | 单条估算大小 |
|------|----------|---------|------------|
| | | | |

> 只填知道的。AI 会问其余的。

---

## 10. 索引与查询模式（如已知）

```
[例：]
- 经常按 (用户ID, 课程ID) 查进度
- 经常按 (是否上架=true, created_at desc) 查课程列表
```

---

## 11. 报表 / 聚合需求

```
[例：每天统计当日新增订单数；每周统计活跃用户]
```

---

## 12. 隐私 / 敏感字段

- `[例：手机号在列表显示要脱敏]`
- `[例：身份证号加密存储]`

---

## 13. 其他

- 与第三方系统的数据同步要求：
- 国际化字段（多语言文案）：
- 不确定的事：
