# 38 · V01 AI 输出：上游链一致性

> **阶段**：D03 · V 一致性校验（最终阶段，开发前）
> **谁产出**：AI（QA / 文档审计师）
> **落盘**：`docs/D03-validation/<feature-id>/01-upstream-chain.md`
> **何时跑**：本 feature C01~D02 全部冻结后启动；V01 → V02 → V03 顺序串跑，全部 ✅ 才进入本 feature 开发。

---

## V01 vs V02 vs V03 分工

| 阶段 | 看哪一层 | 看什么 |
|------|---------|-------|
| **V01** | **全局上游链** | **本 feature R/I/N/H/E（C01~C05）之间引用对齐 + C02 04-pages.md · 03-state-machines.md 与 D02 _global-routes / 03-endpoints 的一一对齐 + R-ID → 页面/原型 覆盖率** |
| V02 | 单模块内 | C02 SM 与 L 接口转移覆盖、D 枚举与 SM 一致、N/H/scenarios 闭环（按 feature 跑）|
| V03 | PRD ↔ 全部规范 | PRD 每段能否回链上游 ID；是否引入规范外内容 |

> V01 是一次性的全局校验；V02 按模块循环；V03 在 PRD 完工后跑。

---

## 触发提示词

```
请你扮演"文档一致性审计师"，只读所有 docs/ 已冻结文件。
按 /prompt/D-develop/D03-V01-AI输出-上游链一致性.md 输出一致性矩阵与冲突清单，
落盘到 docs/D03-validation/<feature-id>/01-upstream-chain.md。
任何不一致都列入"必须修复"清单，不要自行调和。
冲突解决路径：标注应回到哪个上游阶段重出 / 重答澄清。
```

---

## 输出文件骨架

```markdown
<!-- TARGET-PATH: docs/D03-validation/<feature-id>/01-upstream-chain.md -->

# 上游链一致性 · 全局

> **生成时间**：YYYY-MM-DD HH:MM
> **校验范围**：列出本次输入的 docs 路径与版本（hash 或 commit）

---

## 1. R-ID 追踪矩阵

| R-ID | 描述 | I 落点 page-id | I 落点 M-ID | I SM-ID | L 接口 API-ID | L URL | D 实体 | N 页面 | H 原型页 | 状态 |
|------|------|-----------------|--------------|---------|---------------|------|--------|--------|-----------|------|
| R-001 | … | P-001 | M-001 | — | API-1 | / | course | P-001.md | P-001.html | ✅ |
| R-002 | … | (未找到) | — | — | API-3 | — | — | — | — | ❌ |

> 缺口在哪一列就回到对应阶段补。状态列仅对有状态转移的 R-ID 填；无状态则写 —。

---

## 2. 角色矩阵一致性

- P 01-roles 角色集 = { ROLE-USER, ROLE-EDITOR, ROLE-ADMIN }
- C02 05-navigation 引用角色集 = { ... }
- N 各页 §6 角色差异 引用角色集 = { ... }
- L 各接口 角色要求 引用角色集 = { ... }

| 角色 ID | P 定义 | 被 C02 引用 | 被 L 引用 | 被 N 引用 | 不一致 |
|--------|-------|--------------|----------|----------|--------|

---

## 3. 路由表一致性（C02 04-pages ↔ D02 _global-routes / 01-routes-delta）

| page-id | C02 04-pages 名称 | D02 _global-routes URL | 鉴权一致 | 角色可见一致 | 不一致 |
|---------|-----------------|------------------------|---------|----------------|--------|

> 缺任何一个 page-id（双向）均为 ❌。

---

## 4. 状态机引用真实性（C02 03-state-machines ↔ D01 枚举 ↔ L 接口承接）

| SM-ID | C02 状态枚举 | D01 对应 status 枚举 | L 覆盖转移数 / SM 总转移数 | 不一致 |
|-------|--------------|-----------------------|------------------------------|--------|

---

## 5. D ↔ L 字段一致性

| D 字段 | 类型 | L 接口入参出现 | L 校验规则与 D 一致 | 不一致 |
|-------|------|---------------|-------------------|--------|

---

## 6. L ↔ N 接口一致性

| API-ID | N 引用页面 OP-ID | 入参齐 | 角色一致 | 错误码处理覆盖 | 不一致 |
|--------|-------------------|-------|---------|---------------|--------|

---

## 7. X / S ↔ N 视觉一致性

- 抽样 5 个页面，检查是否使用了 docs/B04-design/design-system/01-tokens.md 中的 token（不允许直接 hex / px 硬编码）
- 检查文案是否与 X 04-voice-tone 对照表一致

| 页面 | 硬编码颜色 | 硬编码字号 | 文案违规 |
|------|----------|----------|---------|

---

## 8. NFR 校验

| NFR ID | 指标 | 在哪份文档落实 | 是否落实 |
|--------|------|--------------|---------|

---

## 9. 必须修复清单（阻断 V 通过）

- [ ] FIX-1：R-002 缺 I/N/H 落点 → 回到 C02 阶段补 page-id
- [ ] FIX-2：API-7 入参 `userType` 在 D 没有对应字段 → 回到 D 补 / L 改名
- [ ] FIX-3：page-id P-XXX 在 C02 存在但 _global-routes 未找到 → 回到 L 阶段补
- [ ] FIX-4：SM-XX:T-Y 无任何接口承接 → 回到 L 阶段补

## 10. 建议优化（不阻断）

- ...

## 11. V01 通过判定

- [ ] §1 矩阵无空格 / 无 ❌
- [ ] §2-§7 不一致列为空
- [ ] §9 必须修复清单为空
- [ ] §8 NFR 全部落实

四项全 ✅ → 可进入 V02（按模块跑模块内闭环）。任意一项 ❌ → 不许进 V02。
```

---

## 输出质量自检
- [ ] 11 节齐？
- [ ] 矩阵抽样足够（每个 R-ID 都过、接口至少抽 30%、页面至少抽 30%）？
- [ ] 路由表与 SM 表双向比对？
- [ ] 必须修复清单与上游阶段一一对应？
