# 39 · V02 AI 输出：模块内闭环校验

> **阶段**：D03 · V 一致性校验（最终阶段，开发前）
> **谁产出**：AI（QA / 文档审计师）
> **落盘**：`docs/D03-validation/<feature-id>/02-module-closure.md`
> **何时跑**：每个模块的 D / L / N / H 全部冻结后；可与 V01 并行，但必须在 V03 之前。

---

## 触发提示词

```
请你扮演"模块内闭环审计师"，只读 docs/ 已冻结文件。
按 /prompt/D-develop/D03-V02-AI输出-模块内闭环.md 输出本模块的"数据 → 接口 → 页面 → 原型 → 场景"闭环校验，
落盘 docs/D03-validation/<feature-id>/02-module-closure.md。
任何字段 / API / 控件 / 场景缺口都列入"必须修复"清单，不要自行调和。
```

---

## V01 vs V02 vs V03 分工

| 阶段 | 看哪一层 | 看什么 |
|------|---------|-------|
| V01 | 全局上游链 | 本 feature R/I/N/H/E（C01~C05）之间的引用是否对齐 + 对 B01~B04 全局产物的引用是否真实存在 |
| **V02** | **单模块内** | **D / L / N / H / scenarios 之间的字段/接口/控件/场景闭环（按 feature 分别跑）** |
| V03 | PRD ↔ 全部规范 | PRD 每段是否能回链上游 ID；是否引入了规范外内容 |

> V02 是按模块循环跑的；其它两个是一次性。

---

## 输出文件骨架

```markdown
<!-- TARGET-PATH: docs/D03-validation/<feature-id>/02-module-closure.md -->

# 模块内闭环校验 · <feature-id>

> **生成时间**：YYYY-MM-DD HH:MM
> **校验范围**：列出本模块输入文件与版本（commit hash）
> **依赖**：V01 全局上游链已通过（红线项必须先修完）

---

## 1. D ↔ L 字段一致性

| D 表.字段 | 类型 | 出现于哪些 L 接口 | L 入参/出参 字段名 | 类型一致 | 校验规则一致 | 不一致点 |
|---------|------|----------------|-----------------|---------|------------|---------|
| `course.title` | string(120) | API-course-create / API-course-update | `title` | ✅ | ✅ | — |
| `course.status` | enum(draft/...) | API-course-list (filter) | `status` | ❌ | — | L 漏 enum 值 `archived` |

> 本表抽样规则：D 中所有"必填字段"100% 抽；可空字段抽样 ≥ 30%。

---

## 2. L ↔ N 接口对齐

| API-ID | 被 N 哪一页引用 | 入参完整 | 角色与 P 一致 | 错误码处理覆盖 | 不一致点 |
|--------|---------------|---------|-------------|---------------|---------|

> 本表必含本模块所有 API-ID（100%）。任意一行有 ❌ → 阻断。

---

## 3. N ↔ H 控件对齐

| 页面 page-id | N 中定义的 Block / OP 数 | H 原型实际渲染数 | 缺失 / 多出的控件 | 状态覆盖（默认/空/错/loading/无权限） |
|-------------|----------------------|----------------|----------------|---------------------------------|

> 5 态有任一缺失 → 阻断。

---

## 4. N.scenarios ↔ H 可点通

| SC-ID | 描述（GIVEN/WHEN/THEN）| H 中可否实际点通 | 中断点 |
|-------|----------------------|----------------|-------|

> 任意 SC 不可点通 → 阻断。

---

## 5. C02 状态机 ↔ L 接口 转移覆盖

> 状态机本身在 docs/C02-ia/<feature-id>/03-state-machines.md 定义；D01 仅营造枚举列；本表检查每条 SM 转移是否有接口承接、角色是否一致、是否有不可达状态或死循环。

| SM-ID | 转移 T-X | 起态 | 终态 | C02 规定触发者 | 承接 API-ID | 接口角色与触发者一致 | D01 枚举包含起终态 | 不可达状态 | 死循环 | 不一致 |
|-------|---------|------|------|----------------|--------------|------------------------|----------------------|----------|--------|--------|
| SM-01 | T-01-1 | 待支付 | 已支付 | 系统 | API-pay-callback | ✅ | ✅ | — | — | — |

> 任一 SM 转移未被接口覆盖 → 阻断；任一不可达状态 → 阻断；任一死循环 → 阻断。

---

## 6. 文案与 token 抽样（视觉零硬编码）

> 抽 5 个本模块页面的 H 原型源码，搜 hex / px 硬编码 / 写死文案。

| 页面 | 硬编码颜色 | 硬编码字号 | 文案与 X-04-voice-tone 不一致 | 备注 |
|------|----------|----------|---------------------------|------|

---

## 7. 必须修复清单（阻断 V02 通过）

- [ ] FIX-1：API-course-list 漏 enum `archived` → 回 L 阶段补
- [ ] FIX-2：course-detail 页缺 loading 态 → 回 N / H 阶段补
- [ ] ...

## 8. 建议优化（不阻断）

- ...

## 9. V02 通过判定

- [ ] §1-§5 不一致列为空
- [ ] §7 必须修复清单为空
- [ ] §6 抽样无硬编码 / 无文案违规

三项全 ✅ → 本模块 V02 通过。所有模块 V02 通过 → 进入 V03。
```

---

## 输出质量自检

- [ ] 9 节齐？
- [ ] §1 必填字段 100% 抽样、§2 API 100% 抽样？
- [ ] §3 状态覆盖 5 态全检？
- [ ] §7 每条 FIX 都标了应回到哪个阶段？
- [ ] 单文件 ≤ 1200 行？
