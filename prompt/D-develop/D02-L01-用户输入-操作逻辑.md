# 35 · L01 用户输入：单功能操作与业务逻辑

> **阶段**：L 业务接口（**按 feature 重复**）
> **谁来写**：你
> **写完落盘**：`docs/D02-api/<feature-id>/_input/operations.md`
> **下一步**：贴给 AI（系统消息塞 `A00-01`、`A00-03`、`A00-04`、`D02-L02`、本 feature R 基线、B01 输出、B02 输出、本 feature D01 D），AI 必须按 `D02-L02` 出澄清问题。

---

## 触发提示词

```
请你扮演"接口设计师"，遵循 /prompt/A-framework/A00-01、/prompt/A-framework/A00-03、/prompt/A-framework/A00-04。
上游：本 feature C01 R 基线 + B01 04-api-conventions + B02 全部 + 本 feature C02 IA（重点 04-pages.md 与 03-state-machines.md） + 本 feature C03 页面交互 + D01 D（已冻结）。
本功能 ID：<feature-id>，关联 R-ID：<R-...>
读取我下面贴的 L 用户输入。
本轮回复必须按 /prompt/D-develop/D02-L02-AI澄清-接口提问.md 输出澄清清单，
落盘 docs/A00-meta/questions/L-<feature-id>-questions-round1.md。
<贴下面填好的内容>
```

---

## 1. 功能信息

- **feature-id**：
- **功能名称**：
- **所属 surface**（来自 B01 08-surfaces.md；多端 feature 同名复用时，本输入按 surface 各填一份）：
- **关联 R-ID**：
- **关联 D 实体**：
- **关联 C02 页面清单（page-id）**：来自 docs/C02-ia/<feature-id>/[<surface>/]04-pages.md
- **关联 C02 状态机（SM-ID）**：来自 docs/C02-ia/<feature-id>/[_shared/]03-state-machines.md

---

## 2. 页面期望 URL 映射（你的偏好）

> 不填也可；AI 会依据命名规则生成。填了会被尊重。
> **多端项目**：所有路由必须以 `/api/<surface>/` 起始（B01 08-surfaces §2 强制约束）。

| page-id | 期望 URL | 备注 |
|---------|----------|------|
| P-admin-001 | /api/admin/courses | |
| P-app-002   | /api/app/courses   | |

## 3. URL 命名 / 路径偏好

- 是否全局加 `/api` 前缀：`[多端项目强制是；并强制带 /<surface>/ 段]`
- 多端项目本 surface 路由前缀：`[例：/api/admin]`
- 多语言：`[ ] 不加前缀  [ ] 路径前缀 /:lang/...  [ ] query ?lang=  [ ] 子域名]`

---

## 4. 该功能的所有"操作"清单

> 一行一条，全部列出（增删改查、特殊动作如"提交审核""撤回""导出"）。

```
[例：]

1. 创建课程（管理员）
2. 编辑课程草稿（管理员）
3. 提交课程审核（管理员）
4. 审核通过 / 退回（超管）
5. 上架 / 下架（超管）
6. 课程列表查询（前台用户）
7. 课程详情查询（前台用户）
8. 收藏课程 / 取消收藏（前台用户）
9. ...
```

---

## 5. 每个操作的关键信息

> 对每条操作填一份小卡片。

### 操作 X：<名称>

- **触发者角色**：
- **入口**：`[页面 / 列表行操作 / 后台脚本 / 定时任务]`
- **输入**：`[字段名 + 说明]`
- **业务逻辑**（伪代码 / 大白话）：
  ```
  1. 校验 ...
  2. 查 ...
  3. 写 ...
  4. 通知 ...
  ```
- **输出**：`[返回什么]`
- **失败场景**：`[空数据 / 重复 / 权限 / 第三方挂掉 / 超时 / ...]`
- **副作用**：`[发邮件 / 写日志 / 触发其他操作]`
- **是否需要事务**：`[是 / 否]`
- **是否幂等**：`[是 / 否]`

---

## 6. 列表查询的细节

- **筛选条件**：
- **排序字段**：
- **分页**：`[页大小默认 / 是否游标分页]`
- **是否需要全量导出**：

---

## 7. 异步 / 长任务

- 哪些操作时间可能 > 2s：
- 处理方式：`[同步等 / 异步 + 轮询 / 异步 + WebSocket / SSE]`

---

## 8. 第三方依赖

- 操作 X 依赖什么外部服务：
- 失败兜底：

---

## 9. 限流 / 防刷

- 哪些操作要限流：
- 限制：`[次/分钟 或 次/天]`

---

## 10. 国际化与文案

- 错误提示是否需要分语言：
- 是否需要按用户时区返回时间：

---

## 11. 其他

- 不确定的事：
