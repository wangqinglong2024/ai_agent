# 35 · V01 AI 输出：一致性校验报告

> **阶段**：V 开发前校验
> **谁产出**：AI（QA / 文档审计师）
> **落盘**：`docs/S11-validation/<feature-id>/01-consistency.md`（或全局：`docs/S11-validation/global/01-consistency.md`）

---

## 触发提示词

```
请你扮演"文档一致性审计师"，只读所有 docs/ 已冻结文件。
按 /prompt/S11-V01-AI输出-一致性校验.md 输出一致性矩阵与冲突清单，
落盘到 docs/S11-validation/<feature-id>/01-consistency.md（或 global/）。
任何不一致都列入"必须修复"清单，不要自行调和。
冲突解决路径：标注应回到哪个上游阶段重出 / 重答澄清。
```

---

## 输出文件骨架

```markdown
<!-- TARGET-PATH: docs/S11-validation/<feature-id>/01-consistency.md -->

# 一致性校验报告 · <feature-id>

> **生成时间**：YYYY-MM-DD HH:MM
> **校验范围**：列出本次输入的 docs 路径与版本（hash 或 commit）

---

## 1. R-ID 追踪矩阵

| R-ID | 描述 | I 落点 page-id | L 接口 API-ID | D 实体 | N 页面 | H 原型页 | 状态 |
|------|------|---------------|--------------|-------|-------|---------|------|
| R-001 | … | home | API-1 | course | home.md | home.html | ✅ |
| R-002 | … | (未找到) | API-3 | — | — | — | ❌ |

> 缺口在哪一列就回到对应阶段补。

---

## 2. 角色矩阵一致性

- P 01-roles 角色集 = { ROLE-USER, ROLE-EDITOR, ROLE-ADMIN }
- I 03-navigation-map 引用角色集 = { ... }
- N 各页 §6 角色差异 引用角色集 = { ... }
- L 各接口 角色要求 引用角色集 = { ... }

| 角色 ID | P 定义 | 被 I 引用 | 被 L 引用 | 被 N 引用 | 不一致 |
|--------|-------|----------|----------|----------|--------|

---

## 3. D ↔ L 字段一致性

| D 字段 | 类型 | L 接口入参出现 | L 校验规则与 D 一致 | 不一致 |
|-------|------|---------------|-------------------|--------|

---

## 4. L ↔ N 接口一致性

| API-ID | N 引用页面 | 入参齐 | 角色一致 | 错误码处理覆盖 | 不一致 |
|--------|-----------|-------|---------|---------------|--------|

---

## 5. X / S ↔ N 视觉一致性

- 抽样 5 个页面，检查是否使用了 docs/70 中的 token（不允许直接 hex / px 硬编码）
- 检查文案是否与 X 04-voice-tone 对照表一致

| 页面 | 硬编码颜色 | 硬编码字号 | 文案违规 |
|------|----------|----------|---------|

---

## 6. NFR 校验

| NFR ID | 指标 | 在哪份文档落实 | 是否落实 |
|--------|------|--------------|---------|

---

## 7. 必须修复清单（阻断 V 通过）

- [ ] FIX-1：R-002 缺 I/N/H 落点 → 回到 I 阶段补 page-id
- [ ] FIX-2：API-7 入参 `userType` 在 D 没有对应字段 → 回到 D 补 / L 改名

## 8. 建议优化（不阻断）

- ...

## 9. V 通过判定

- [ ] §1 矩阵无空格 / 无 ❌
- [ ] §2-§5 不一致列为空
- [ ] §7 必须修复清单为空
- [ ] §6 NFR 全部落实

四项全 ✅ → 进入 V2（Story 拆分）。任意一项 ❌ → 不许进 V2。
```

---

## 输出质量自检
- [ ] 9 节齐？
- [ ] 矩阵抽样足够（每个 R-ID 都过、接口至少抽 30%、页面至少抽 30%）？
- [ ] 必须修复清单与上游阶段一一对应？
