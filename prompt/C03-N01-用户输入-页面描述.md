# 30 · N01 用户输入：单页面描述

> **阶段**：N 页面交互（**按 page 重复**）
> **谁来写**：你
> **写完落盘**：`docs/S09-pages/<feature-id>/_input/<page-id>.md`
> **下一步**：贴给 AI（系统消息塞 `S00-01`、`S00-03`、`S00-04`、`S09-N02`、R 基线、P、D、L 对应 feature、X、S、I），AI 必须按 `S09-N02` 出澄清问题。

---

## 触发提示词

```
请你扮演"页面交互设计师"，遵循 /prompt/S00-01、/prompt/S00-03、/prompt/S00-04，硬性遵守 docs/S06-ux/ 与 docs/S07-design-system/。
本页：page-id=<>，feature-id=<>。
上游：R 基线、P、I 02-pages、对应 D、对应 L、X、S（已冻结）。
读取我下面贴的 N 用户输入。
本轮回复必须按 /prompt/S09-N02-AI澄清-交互提问.md 输出澄清清单，
落盘 docs/S00-meta/questions/N-<page-id>-questions-round1.md。
<贴下面填好的内容>
```

---

## 1. 页面身份

- **page-id**（来自 I 02-pages）：
- **路径**：
- **关联 R-ID**：
- **关联 D 实体**：
- **关联 L API-ID**（这页会调哪些接口）：

---

## 2. 这个页面的"用户来这里要干嘛"

> 一句话。比"展示课程列表"更具体——"快速找到能在通勤路上学的 30 分钟以内的课程"。

```
```

---

## 3. 页面骨架（粗到 5-7 个区块）

> 用块名 + 一句话职责。不要画图，AI 会画。

```
[例 课程详情页：]

1. 顶部封面区：大标题 + 副标题 + 价格 + 主 CTA
2. 课程信息条：讲师 / 难度 / 时长 / 语言
3. 章节列表：可折叠，免费试看标识
4. 评价区：星级分布 + 评论列表
5. 推荐课程：4 张卡
6. 底部固定 CTA：移动端常驻
```

---

## 4. 用户能在这页做的所有操作

> 列全。每条标"主/次"。

```
1. (主) 加入课程
2. (次) 收藏
3. (次) 分享
4. (次) 试看免费章节
5. (次) 提交评价（已购后）
6. (次) 切换章节查看
```

---

## 5. 数据来源

- 进入页时拉的接口：`API-X (GET /api/...)`
- 操作触发的接口：

---

## 6. 状态需要表达的 4 类（必填）

| 状态 | 触发条件 | 应表达 | 主操作 |
|------|---------|-------|-------|
| 默认 | 正常加载完 | | |
| 加载中 | 数据未到 | Skeleton/Spinner | — |
| 空数据 | 列表为空 / 无权限可看 | 提示 + 引导 | |
| 错误 | 接口失败 | 错误信息 + 重试 | |

---

## 7. 角色差异

- 未登录看到什么：
- ROLE-USER 看到什么：
- ROLE-EDITOR 看到什么：
- ROLE-ADMIN 看到什么：
- 已购 vs 未购：
- 自己 vs 别人：

---

## 8. 关键场景（**重要**）

> 用 GIVEN/WHEN/THEN 写至少 5 个。AI 会用这些做交互验证。

```
SC-1: GIVEN 未登录用户进入课程详情
       WHEN 点击"加入课程"
       THEN 弹登录提示，登录成功后回到当前页继续

SC-2: GIVEN 已购用户进入
       WHEN 点击"开始学习"
       THEN 跳转到 /learn/<lesson-id> 进入第一节

SC-3: GIVEN 列表数据为空
       WHEN 进入页面
       THEN 显示空态："暂无课程，去看看推荐" + 按钮跳转推荐页

SC-4: GIVEN 接口超时
       WHEN 进入页面
       THEN 显示错误态 + 重试按钮，非全屏

SC-5: GIVEN 移动端
       WHEN 进入页面
       THEN 主 CTA 固定在底部
```

---

## 9. 表单（如有）

- 必填字段、校验规则、错误展示位置（行内 vs 顶部 vs Toast）
- 提交方式（同步 / 异步 + 进度）
- 离开页面是否需"未保存"提示

---

## 10. 移动端要求

- 关键改变：
- 隐藏 / 折叠的元素：

---

## 11. 性能与加载

- 是否需要骨架屏：
- 是否懒加载图：
- 首屏 LCP 目标：

---

## 12. SEO（如适用）

- 必要 meta：
- title 模板：

---

## 13. 不确定的事

```
```
