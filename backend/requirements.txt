# FastAPI 核心
fastapi==0.115.*
uvicorn[standard]==0.34.*

# Supabase Python SDK
supabase==2.*

# JWT 验证 (验证 Supabase 发出的 JWT)
PyJWT==2.*
cryptography==44.*

# HTTP 客户端 (调用 Dify / Openclaw API)
httpx==0.28.*

# SSE 流式响应
sse-starlette==2.*

# 环境变量管理
pydantic-settings==2.*

# 工具
python-multipart==0.0.*
